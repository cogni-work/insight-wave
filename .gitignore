excalidraw.log
*.log
__pycache__/

# Secrets
.env
.env.*

# Claude Code local settings
.claude/settings.local.json
.claude/worktrees/
.claude/cogni-docs-state.json

# macOS
.DS_Store

# Node
node_modules/

# Skill eval workspaces (large run outputs, dev-time artifacts)
*-workspace/
!cogni-workspace/

# Eval run artifacts but NOT eval definitions (evals.json tracks test prompts + expectations)
**/evals/fixtures/
**/evals/outputs/

# Eval iteration outputs
cogni-portfolio-evals/

# Diagram test pipeline (closed-loop test artifacts)
cogni-research/test-diagram-pipeline/

# Render agent scratch files (canvas-clearing temp scenes, etc.)
.tmp/

# Obsidian vault config (per-user; cogni-workspace:manage-workspace scaffolds it)
.obsidian/

# cogni-wiki batch-mode lockfile (runtime-only, per-wiki advisory lock)
**/.cogni-wiki/.lock

# cogni-help local issue queue (per-user draft issues, not shared state)
/cogni-issues/

# cogni-knowledge Phase 4 alpha scratch (per-user research bases, not shared state)
/.alpha/

# Inverted-pipeline per-project fan-out + manifest intermediates (per-user, regenerable):
# cogni-knowledge verify-shards + manifests, cogni-portfolio scan-output. /.alpha/ above
# covers the in-repo alpha base; this covers a knowledge/portfolio project created elsewhere.
**/.metadata/
