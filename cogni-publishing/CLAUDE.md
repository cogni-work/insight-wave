# cogni-publishing

Standalone publishing contracts and validation for insight-wave. The plugin must work without cogni-workspace — or any other insight-wave plugin — being installed or initialised.

## Architecture

The artifact chain is deliberately split so each stage evolves on its own version:

1. authored input — `direct-brief@1`, or the bounded `design-brief@1.1` slides adapter
2. `normalized-brief@1` — the only place copy and data live
3. `semantic-composition@1` — grouping and roles, by reference
4. `target-resolved-plan@1` — target and design-system decisions, by reference

Beside that chain, `design-compose` binds a normalized brief to `pattern-library@1` — the bundled `references/pattern-library-v1.json` — and emits a `semantic-composition@2`: pattern-bound units that reference content by id and digest and carry no copy and no target geometry. `target-resolved-plan@1` still consumes `@1` only. `design-render` lays a `@2` composition out as a `target-resolved-plan@2` — geometry and typography per slot, content by the composition's own references — and renders it. Its layering: `scripts/design-render.py` is the stdlib wrapper and the only entry point, `scripts/render_core.py` the target-neutral core (inputs, theme, fonts, plan, provenance), and each target is a sibling adapter behind them with its own independent checks — `scripts/html_adapter.py` with `scripts/render_checks.py` for `html`, and `scripts/pptx_adapter.py`, a stdlib Office Open XML writer, with `scripts/pptx_checks.py` for `pptx`. A deck is written straight from the plan and records every object's editability in a `pptx-manifest@1`; neither adapter imports the other. `references/design-render.md` is normative for the render.

`design-verify` is the verification layer on top of the render: `scripts/design-verify.py` is its stdlib wrapper and `scripts/verify_checks.py` its checks. It imports the render path and reuses its readers, but derives every expectation from the frozen normalized brief, the composition, the pattern library, the theme and `references/verify-capabilities.json`; the render path imports nothing from it, so the render-path file lists `drnd-02`, `drnd-03`, `drnd-04` and `drnd-36` name do not include it. `render-verified` calls `design-render.py`'s `cmd_render` in-process and repairs only through the validator's `check-repair`. `references/design-verify.md` is normative for verification, and `docs/design-verify-proof.md` with `docs/design-verify-proof/` is the committed two-brand proof.

`references/artifact-contracts.md` is normative for the chain and `references/design-composition.md` for the pattern contract and binding rules; the JSON Schemas beside them document the same shapes. `scripts/validate-publishing.py` is the single enforcer.

## Conventions

- Scripts use Python 3 stdlib only and emit exactly one `{"success": bool, "data": {...}, "error": string|null}` on stdout and nothing on stderr. Exit 0 valid, 1 contract rejection, 2 usage/runtime error. A rejection's `data` is the finding (`code`, `check`, `artifact`, `reference`) and never a partial artifact.
- Never copy text into a downstream artifact. Composition and plan units reference normalized records by id; the validator rejects any other unit field as `unexpected-field`.
- Normalization never rewrites, reorders or drops a value. Narrative records keep their exact source slice as `raw`.
- The narrative adapter stays bounded to the slides grammar of `design-brief@1.1`. Widening it to another target or version is a contract change, not a parser tweak.
- A direct brief keeps its declared `structure`; never add arc, BLUF, slide-number or narrative-element requirements to it.
- Read workspace preferences only from a caller-supplied path. The validator never reads environment variables or the home directory and never probes sibling plugin paths; `pubc-16` in the suite enforces this with an audit hook.
- Resolve configuration per key: supplied values, publishing project configuration, optional workspace preferences, bundled defaults.
- Rendering stays optional and outside validation. An external renderer is pinned with an exact version and consumes `target-resolved-plan@1`; validation checks the pin as data and never installs, imports or runs a renderer.
- `design-render` renders both targets in Python 3 stdlib, and the render path — the PPTX writer and its checks included — must compile on Python 3.9, the repo's floor — so no nested same-quote f-strings, `match` statements or runtime `X | Y` unions. Its browser runtime under `runtime/` only measures a page, is pinned by `runtime/package-lock.json` (exact versions, sha512 integrity), is provisioned only by running `runtime/provision.sh` — once per machine by an operator, or in CI by its own step of the Plugin test suites job ahead of the sweep — and is resolved only from its provisioning record: never from PATH, and never installed by the wrapper, a skill or a suite. Keep `npm`, `npx`, `pip` and `curl` out of the wrapper and render path.
- Every original string reaches the page through the adapter's one insertion function, `text()`, whose body must stay exactly `return escape(value, quote=True)` — the recorded `drnd-10` recipe mutates that line. Attribute values use `attr()` instead, so the recipe hits copy only.
- The PPTX writer has the same shape: every original string reaches the package through `xml_text()`, whose body must stay exactly `return escape(value)` and occur once in `pptx_adapter.py` — the recorded `drpx-06` recipe mutates it — and attribute values use `xml_attr()`. The native chart is placed by the single call line `shapes.append(self.native_chart(unit, slot, entries))`, and `self.native_chart(` must occur exactly once in the file, because the recorded `drpx-09` recipe swaps it for `self.data_table(`.
- A deck never shrinks, splits, merges, truncates or reorders a unit to fit its fixed slide: a frame the plan grew past 720 px fails as `fit-overflow`, copy a package cannot hold fails as `unsupported-content`, and no text body carries `a:normAutofit` or a `fontScale`. A picture may enter a deck only as the fallback its unit's variant declares for pptx in the pattern library, read from the library, carrying no copy and recorded in the manifest; a deck without one keeps its exact bytes, so the picture media types are written only when a picture is.
- The render validates everything, runs the fidelity checks on its own page, and only then writes; a rejection writes nothing. Never patch a rendered page, plan or provenance by hand — re-render from the inputs.
- Component CSS in the adapter uses theme tokens through `var()` only, and nothing may hide, clamp or clip copy. A theme may ship licensed faces, one per weight of a family, declared in its `assets/fonts/faces.json`: the html target embeds every face of the copy family as its own `@font-face` data URI ahead of the token block, each with a `font-weight` descriptor equal to its declared weight — the only `url()`s the portability check admits, and each only when its bytes equal the file the theme ships under that family and weight — while a deck embeds no font. Layout measures with the copy face, the one matched for weight 400. Any other face resolves to a generic chain in `references/font-fallbacks-v1.json` and is recorded as a substitution; `bundled_faces` stays empty, because a face ships with the theme that uses it. A font file enters the tree only with its licence beside it and a root `NOTICE` entry whose `Location:` and `See:` lines resolve.
- Add a contract version only with a compatibility row in `references/artifact-contracts.md` and the matching `SUPPORTED`/`COMPATIBLE` entries in the validator, in the same change.
- Patterns are library data; families (`text`, `chart`, `system`, `register`) are validator code. Adding a pattern is a library entry with at least one passing specimen; adding a family is a reviewed code change.
- A `proposed` pattern never reaches production: the composition commands reject it as `unaccepted-pattern`. Promotion is a reviewed change that flips its `status` once `check-patterns` reports it ready.
- `compose` fills only mechanical fields — digests, the content fingerprint, citations, the register and trailer-note bindings — and never picks, splits, merges, truncates or reorders content. A repair changes only pattern, variant and slot names.
- The composition commands read the bundled library beside the script, found from the script's own location, or an explicit `--patterns` file — nothing else.

## Theme lifecycle

This plugin is the single owner of the theme lifecycle: `manage-themes` (selection, authoring, audit, showcase, application and Claude Design import), the bundled `themes/`, and the scripts, schema and references behind them. `cogni-workspace` keeps only compatibility routes — a same-name `manage-themes` skill that delegates here and four script entry points resolved through its `scripts/_publishing_delegate.py` — so never add theme behaviour there; add it here and let the routes forward it.

- **Roots come from the script's own location.** The theme scripts find the bundled `themes/` as `<plugin>/themes` relative to themselves, with `--plugin-root` as an explicit override. They never read `$CLAUDE_PLUGIN_ROOT` for it: a caller in another plugin — or a delegate — runs with that variable naming a plugin that ships no themes.
- **User themes are input, never output of a read.** The optional user location resolves `--user-themes` > `--workspace-root`/themes > `$COGNI_WORKSPACE_ROOT/themes` > legacy auto-discovery (`discover-themes.py` only), and a user theme shadows a bundled one of the same slug. No read moves, rewrites or creates anything there; only the write operations of `manage-themes` do.
- **The selection handoff is `theme_path` / `theme_name` / `theme_slug`**, emitted by `scripts/select-theme.py` and defined in `references/theme-artifact-contract.md`. Its explicit-path mode reads no environment and nothing under `$HOME`.
- **Legacy output shapes are part of the contract.** `discover-themes.py` prints a bare JSON array with `standard`/`workspace` source labels, and its tier-0 output is snapshotted in `scripts/baselines/`; `inspect-themes.py` and `check-theme-drift.py` keep their envelopes. The envelope-only, nothing-on-stderr rule above binds the contract scripts and `select-theme.py`; the legacy theme scripts keep their existing stderr hints.
- **`tokens/*.json` is the one authoritative token representation.** `generate-tokens-css.py` compiles it to `tokens.css` (aliases stay `var()` references) and, when an alias exists, `tokens.resolved.json`. Both are projections: regenerate them, never edit them. The token header line and flat-map output are frozen, because every saved `tokens.css` is parity-checked against them. The accepted input shapes are `references/token-subset.md`; a `$type` added to the compiler's `SUPPORTED_TYPES` must be added to that page's `**Supported $type values:**` line in the same change, which `stok-19` pins.
- **A tier-0 theme's tokens are derived, never transcribed.** `scripts/derive-theme-tokens.py` writes `colors.json`, `typography.json` and `spacing.json` from a `theme.md` — every value verbatim from its row, no default and no hex of its own — and has the compiler write `tokens.css`. The `boardroom`, `clean-slate`, `editorial` and `signal` presets ship that output, so after editing one of their `theme.md` files re-run `derive-theme-tokens.py themes/<slug> --overwrite`; never edit their token JSON. The renderer keeps reading only `tokens/*.json`: a role a `theme.md` lacks stays absent and fails as `theme-token-missing`, and nothing in the render path parses `theme.md` prose or fills a role. Without `--overwrite` the derivation refuses any target that already holds token files, which is what protects `cogni-work`'s imported tokens.
- **The importer never writes around a broken graph.** A bundle alias cycle or unresolved reference aborts before the target is touched; unsupported alias forms are reported in `aliases_dropped`, never dropped silently. Re-import replaces only the files the sidecar's `managed_files` lists.

## Tests

`tests/test-publishing-contracts.sh` is discovered by `scripts/run-plugin-tests.py`. Case ids are `pubc-NN-<discriminator>`, allocated once and never renumbered — the mutation recipes in the suite header and README record `pubc-04-invalid-version` and `pubc-05-dangling-reference`. The two predicates those recipes mutate, `version_supported` and `reference_resolves`, must keep their exact one-line bodies, or the recorded `--expr` stops matching.

`tests/test-design-compose.sh` grades the composition layer with `dcmp-NN-<discriminator>` ids under the same allocate-once rule. Its recorded recipes mutate `order_preserved`, `provenance_present` and `pattern_accepted` in the validator, which must keep their exact one-line bodies — `return positions == sorted(positions)`, `return len(source_refs) > 0` and `return pattern.get("status") == "accepted"` — the register guard in `validate_composition`, which must stay the single line `if require_register and index.source_ids and not state.register_units:` — and the routing sentence in `skills/design-compose/SKILL.md` that begins "Never route a proposed pattern into a production composition", which must occur exactly once. `pubc-16` also audits the four composition commands for standalone isolation. The two composition fixtures are frozen `compose` output: regenerate them with `compose` from a stripped draft, never by typing a digest.

`tests/test-design-render.sh` grades the render with `drnd-NN-<discriminator>` ids under the same allocate-once rule. Its six recorded recipes mutate the adapter's `text()` body, which must fail `drnd-10-frozen-copy`; the single line `if node.tag == "style":` in `render_checks.py`'s `reference_surfaces`, which must fail `drnd-37-prose-paths-render`; the two embedded-face gates in `render_checks.py`, `if shipped_family != family:` and `if face is None or face["format"] != form or face["mime"] != mime:`, each of which must fail `drnd-50-embedded-face-negatives`; and the weight gate after them, `if shipped_face is not None and shipped_face["weight"] != face["weight"]:`, which must fail `drnd-59-bold-as-400` — so each of those four lines must keep its exact text and occur once in the file; and the copy-face selector in `render_core.py`'s `copy_face`, the single line `near = [weight for weight in by_weight if COPY_WEIGHT <= weight <= 500]`, which must fail `drnd-63-copy-face-only-450` — so that line, too, must keep its exact text and occur once in its file. The weight gate skips an unknown payload on purpose: that is what leaves each drnd-50 recipe's negative admitted rather than caught a second time. Its expected strings come from the fixture inputs through the suite's own html.parser reader, never from a render; a case that needs an edited brief recomposes from a stripped draft with `compose`. `drnd-34`, `drnd-35` and `drnd-53` need the pinned runtime: without it they print `SKIP:` naming the absent runtime and never `PASS:`. `drnd-36` compiles the render path with the oldest Python 3.9–3.11 interpreter on the host and prints `SKIP:` when there is none. The Plugin test suites job in `.github/workflows/lint.yml` provisions both in their own steps before the sweep — the runtime by running `runtime/provision.sh`, and a Python 3.9 exposed as `python3.9` beside the runner's newer `python3` — and sets `COGNI_PUBLISHING_REQUIRE_PROVISIONED=1`. Under that variable each of those `SKIP:` arms prints `FAIL:` under the same id, so CI cannot go green with them skipped; a local run without it keeps the `SKIP:` arms. `drnd-02`, `drnd-03`, `drnd-04` and `drnd-36` name the render-path files one by one, the PPTX modules among them, so a new render module joins those lists or escapes their guards; `drnd-31` proves an unowned target is refused. The render fixtures live in `tests/fixtures/render/`: `composition-direct-de-edge-v2.json` is frozen `compose` output under the same regenerate-never-type rule; `themes/cogni-work/` is a fixed copy of the test theme, so captured geometry never moves with the bundled theme; and `plan-narrative-captured-v2.json` is a captured render of the narrative fixture with that theme — regenerate it only by rendering, and only when the layout rules change on purpose. `font-theme/cogni-work/` is that theme with one change, a copy font that leads with Outfit, which it ships under `assets/fonts/` beside its `OFL.txt`; its declaration names no weight, so it is the default-400 case `drnd-56` proves, and a case that needs a variant copies the theme into a scratch directory rather than committing another binary. Its declared `advance_em` is derived from the file, and `drnd-49` re-derives it from the sfnt tables. The tree's only other font binaries are the bundled theme's `themes/cogni-work/assets/fonts/DMSans-Regular.otf` and `DMSans-Bold.otf`, unmodified static instances from `googlefonts/dm-fonts` beside their `OFL.txt`: `drnd-54` through `drnd-63` grade them, `drnd-62` re-derives each face's `advance_em` by the same method, and the negatives work on scratch copies. The root `NOTICE` names every one of these files by path in `Location:`/`See:` lines, so moving any of them means moving its line with it.

`tests/test-design-render-pptx.sh` grades the PPTX target with `drpx-NN-<discriminator>` ids under the same allocate-once rule, and is also the documented capability test behind the stdlib writer that replaces PptxGenJS. From the package itself it proves each of the six pptx capabilities pattern-library@1 declares: `text-frame` by `drpx-06-frozen-copy`, `hyperlink` by `drpx-13-citations-and-order`, `editable-shapes` by `drpx-11-editable-shapes`, `native-chart` by `drpx-09-native-chart`, `speaker-notes` by `drpx-12-notes-evidence` and `picture-fallback` by `drpx-27-fallback-picture`. That mapping is stated normatively in `references/design-render.md`; keep every copy of it listing all six. Its four recorded recipes mutate `xml_text()`'s body, which must fail `drpx-06-frozen-copy`; the `self.native_chart(` call, which must fail `drpx-09-native-chart`; the per-variant fallback gate in `pptx_checks.py`, the single line `if fallback != declared_fallback(slide.name, units, library):`, which must fail `drpx-29-per-variant-gate` — so that line must keep its exact text and occur once in the file; and the shared chart row in `render_core.py`'s `series_rows`, the single line `return [(lines, tallest) for lines, _ in measured]`, which must fail `drpx-33-wrapped-chart-alignment` — so that line, too, must keep its exact text and occur once in its file. Every point of a chart takes that one tallest row because a native bar chart spreads its categories evenly over its frame: rows sized per label put the bars of a chart with one wrapped label off their rows, which `drpx-33` measures from the package. Its expectations come from the fixture inputs through the suite's own zipfile/ElementTree reader, never from a render and never through `pptx_checks.py`; its negatives are doctored copies of a green deck. Two of those rules are easy to weaken by accident. The readability floor is per slot: `pptx_checks.py` resolves each copy shape's type role from the composition, the pattern library and the theme through `render_core`'s `SLOT_ROLES` and `TYPE_ROLE_TOKENS` — never from the writer — and keeps the caption size only as a backstop, which `drpx-17`'s shrunk-headline negative distinguishes from the per-slot rule. And a picture is rejected as `unreported-flattening` on three separate arms — no manifest entry, an entry claiming native editability, a fallback entry without capability or reason — each with its own `drpx-14` negative, so dropping one clause turns that case red; the one real fallback adds a fourth arm, a picture missing from the manifest's top-level `fallbacks`, with its own `drpx-30` negative beside the per-variant `undeclared-fallback` gate that `drpx-29` proves. That fallback is rendered from `tests/fixtures/render/direct-loop-v1.json` and `composition-direct-loop-v2.json`, frozen `compose` output under the regenerate-never-type rule, which `drpx-32` re-derives. The wrapped-label chart that `drpx-33` renders comes from `tests/fixtures/render/direct-wrap-v1.json` and `composition-direct-wrap-v2.json` — the costs brief with one label that wraps — under the same rule, which `drpx-34` re-derives. Rendering a deck needs no runtime, so the suite has no `SKIP:` arm: every case runs wherever the suite runs, CI included. `docs/pptx-smoke-evidence.md` records the application open and edit evidence CI cannot produce. It labels a check no person has performed yet as pending rather than omitting it, and no other surface may claim an application result that file does not record.

`tests/test-design-verify.sh` grades the verification layer and the committed proof with `dver-NN-<discriminator>` ids under the same allocate-once rule.

- **Recorded executable recipes.** Three mutate the one-line predicates in `verify_checks.py` — `return found_text == frozen_text` (`copy_preserved`), `return target_url == source_url` (`link_preserved`) and `return needed_px > available_px + CLIP_TOLERANCE_PX` (`clipped`) — which must fail `dver-11-frozen-copy`, `dver-12-source-link-loss` and `dver-13-clipping`. A fourth mutates `return used < budget` (`budget_left`) in `design-verify.py`, which must fail `dver-25-repair-budget-zero`. Each of those four lines must keep its exact text and occur once in its file.
- **Recorded prose recipes.** Nine delete an anchored line. Six are in `skills/design-verify/SKILL.md`: the five review rules that begin "Inspect every unit at full resolution", "Review one deck overview per brand and target", "An open critical finding blocks success", "Never record an unqualified quality verdict" and "A repair never alters frozen content", and the line that begins "The repair budget defaults to 3". Three are in `skills/design-render/SKILL.md`: the lines that begin "After every render, run design-verify on the output and report success only when its verdict passes", "When verification fails, repair within the budget" and "Never rewrite, shorten, add, drop or reorder copy". A tenth deletes only the clause " and report success only when its verdict passes" from design-render's post-render line and must fail `dver-34-render-wiring`. Each must start its line and occur exactly once.
- **Frozen fixtures.** The proof fixtures in `tests/fixtures/verify/` are frozen: `direct-proof-v1.normalized.json` is `normalize` output, and both `composition-proof-*-v2.json` and `composition-unfit-v2.json` are `compose` output from a stripped draft, which `dver-05` re-derives. Regenerate them, never type a digest.
- **The committed proof.** Everything under `docs/design-verify-proof/` is rendered or verified output, bound by `proof-manifest.json`. `dver-06` recomputes its digests, `dver-07` re-verifies every output and compares its committed report, `dver-43` re-renders all four outputs into a scratch directory, byte-compares each page and deck, compares each plan and runs `check-provenance` on each committed bundle, and `dver-24` compares the committed repair record. So a change to the writer, a bundled brand's tokens or the proof fixtures means re-recording the proof from the commands in `docs/design-verify-proof.md`, with a fresh visual review, in the same change. Never patch a recorded digest.
- **Recorded versions are history.** The renderer version and brand pin the proof records are those it was rendered at; no case compares them with the live `plugin.json`.

The suite needs no runtime and has no `SKIP:` arm.

The narrative fixture is cogni-workspace's green `slides-en.md` design brief plus one `evidence_status` line on slides 2–6; its expected normalized output is frozen in `narrative-slides-v1.expected.json`. When the adapter's output shape changes on purpose, regenerate that file and confirm `pubc-02` — the independent slicing oracle — still passes before committing it.

The theme suites follow the same id discipline: `test-theme-lifecycle.sh` (`thl-NN-…`), `test-semantic-tokens.sh` (`stok-NN-…`), and the moved `test-theme-backcompat.sh` (`tbc…`), `test-bundled-presets.sh` (`bp-…`) and `test-check-contrast.sh` (`cc…`). The alias recipe in the `test-semantic-tokens.sh` header mutates `retains_alias` in the importer, so that function must keep its exact one-line body, `return ALIAS_VAR.fullmatch(value) is not None`.

`tests/test-derive-theme-tokens.sh` grades the tier-0 derivation and the bundled themes it makes renderable with `dtt-NN-<discriminator>` ids under the same allocate-once rule, each per-theme id carrying the theme slug. Per preset it re-derives the committed `theme.md` into a scratch directory and byte-compares every token file (`dtt-03`), and checks idempotency, renderer-role coverage, verbatim literals, the Border outcome and font order; for all five bundled themes it runs a real `render --target html` and reads the font records in provenance; it also covers the missing-role fixture in `tests/fixtures/derive/`, the refusal to overwrite existing tokens, malformed and repeated rows and heading scope. Its oracles read `theme.md` with their own section slicer, never the derivation's parser. The recorded recipe mutates `"Background": "bg"` in `derive-theme-tokens.py`, which must fail `dtt-03-rederive-boardroom`, so that substring must occur exactly once in the file.

```bash
python3 scripts/validate-publishing.py normalize --kind narrative --input tests/fixtures/narrative-slides-v1.md
python3 scripts/validate-publishing.py validate --input tests/fixtures/contract-chain-v1.json
python3 scripts/validate-publishing.py check-composition --brief tests/fixtures/narrative-slides-v1.expected.json --composition tests/fixtures/composition-narrative-v2.json
python3 scripts/design-render.py render --target html --brief tests/fixtures/narrative-slides-v1.expected.json --composition tests/fixtures/composition-narrative-v2.json --theme tests/fixtures/render/themes/cogni-work --out /tmp/render
python3 scripts/design-render.py render --target pptx --brief tests/fixtures/narrative-slides-v1.expected.json --composition tests/fixtures/composition-narrative-v2.json --theme tests/fixtures/render/themes/cogni-work --out /tmp/render-pptx
bash tests/test-publishing-contracts.sh
bash tests/test-design-compose.sh
bash tests/test-design-render.sh
bash tests/test-design-render-pptx.sh
bash tests/test-design-verify.sh
python3 scripts/design-verify.py check-proof --manifest docs/design-verify-proof/proof-manifest.json
```
