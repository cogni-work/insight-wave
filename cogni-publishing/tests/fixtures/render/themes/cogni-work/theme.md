# Cogni Work (render fixture)

A fixed copy of the design-render test theme. Its slug matches the design-system name the
composition fixtures pin, and its tokens never change with the bundled theme, so rendered token
values and captured geometry stay comparable across plugin releases.

The copy font names a brand face that no theme or plugin bundles, followed by generic families, so
every render of this theme exercises an explicit, recorded font substitution.
