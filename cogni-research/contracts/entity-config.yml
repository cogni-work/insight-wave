version: "1.1.0"
script:
  name: "entity-config.sh"
  path: "scripts/lib/entity-config.sh"
  purpose: "Bash interface to centralized entity schema configuration"
  category: "lib"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success (functions return normally)"
    1: "Error (jq not found, config file not found, or invalid input)"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "1.1.0"
    date: "2026-01-04"
    changes: "Initial contract generation"
    breaking: false
