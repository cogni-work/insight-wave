version: "1.2.0"
script:
  name: "entity-lock-utils.sh"
  path: "scripts/entity-lock-utils.sh"
  purpose: "Provide mkdir-based advisory locks for atomic entity creation operations"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "1.2.0"
    date: "2025-11-18"
    changes: "Initial contract generation"
    breaking: false
