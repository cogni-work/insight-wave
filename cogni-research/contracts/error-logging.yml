version: "1.0.0"
script:
  name: "error-logging.sh"
  path: "scripts/utils/error-logging.sh"
  purpose: "Reusable error logging utility for deeper-research pipeline scripts"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "1.0.0"
    date: "2025-11-09"
    changes: "Initial contract generation"
    breaking: false
