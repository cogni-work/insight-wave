version: "2.0.0"
script:
  name: "post-entity-creation.sh"
  path: "hooks/post-entity-creation.sh"
  purpose: "Validate entity structure after creation (YAML, wikilinks, UUID)"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "2.0.0"
    date: "2025-11-21"
    changes: "Initial contract generation"
    breaking: false
