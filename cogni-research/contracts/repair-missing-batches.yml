version: "1.0.0"
script:
  name: "repair-missing-batches.sh"
  path: "hooks/repair-missing-batches.sh"
  purpose: "Detect and repair missing batch files after findings-creator"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success (no repairs needed OR repairs completed)"
    1: "Error during repair (logged but doesn't block)"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "1.0.0"
    date: "2025-12-08"
    changes: "Initial contract generation"
    breaking: false
