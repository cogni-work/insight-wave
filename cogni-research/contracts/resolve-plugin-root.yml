version: "1.0.0"
script:
  name: "resolve-plugin-root.sh"
  path: "scripts/utils/resolve-plugin-root.sh"
  purpose: "Resolve CLAUDE_PLUGIN_ROOT to correct plugin directory"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
    110: "CLAUDE_PLUGIN_ROOT not set and cannot be derived"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "1.0.0"
    date: "2025-12-31"
    changes: "Initial contract generation"
    breaking: false
