version: "0.0.0"
script:
  name: "test-partition-entities.sh - Comprehensive test suite for partition-entities.sh"
  path: "scripts/test-partition-entities.sh"
  purpose: "Validate partition script behavior across all edge cases"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "0.0.0"
    date: "2025-11-18"
    changes: "Initial contract generation"
    breaking: false
