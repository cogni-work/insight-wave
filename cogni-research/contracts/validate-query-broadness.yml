version: "1.0.0"
script:
  name: "validate-query-broadness.sh"
  path: "scripts/validate-query-broadness.sh"
  purpose: "Analyze search query complexity and estimate success probability"
  category: "utilities"
interface:
  parameters:
    - name: "--help"
      type: "string"
      required: false
      description: "Display usage information"
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "1.0.0"
    date: "2025-11-09"
    changes: "Initial contract generation"
    breaking: false
