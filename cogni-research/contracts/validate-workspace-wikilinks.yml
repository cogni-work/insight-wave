version: "2.0.0"
script:
  name: "validate-workspace-wikilinks.sh"
  path: "hooks/validate-workspace-wikilinks.sh"
  purpose: "Detect and reject bare filename wikilinks in multi-project Obsidian workspaces"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "2.0.0"
    date: "2025-11-21"
    changes: "Initial contract generation"
    breaking: false
