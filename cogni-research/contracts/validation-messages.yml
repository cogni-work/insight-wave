version: "1.0.0"
script:
  name: "validation-messages.sh"
  path: "scripts/utils/validation-messages.sh"
  purpose: "Centralized error/warning message catalog for template integration validation"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "1.0.0"
    date: "2025-11-20"
    changes: "Initial contract generation"
    breaking: false
