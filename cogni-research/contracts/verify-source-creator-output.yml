version: "2.0.0"
script:
  name: "verify-source-creator-output.sh"
  path: "hooks/verify-source-creator-output.sh"
  purpose: "Validates source-creator output against filesystem reality"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Validation passed or recovery succeeded"
    1: "Recovery failed"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "2.0.0"
    date: "2025-12-21"
    changes: "Initial contract generation"
    breaking: false
