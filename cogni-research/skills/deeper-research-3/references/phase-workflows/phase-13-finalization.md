# Phase 13: Finalization

Generate compliance report and complete research workflow.

---

## ⛔ PHASE ENTRY VERIFICATION (MANDATORY)

**Self-Verification:** Before running bash verification, check TodoWrite to verify Phase 12 is marked complete. Phase 13 cannot begin until Phase 12 todos are completed.

**THEN verify Phase 12 completion (wikilink validation passed):**

```bash
# Verify Phase 12 artifacts exist and wikilink validation passed
ls -la research-hub.md

# Verify no broken wikilinks remain (should return 0 or empty)
broken_count=$(bash "${CLAUDE_PLUGIN_ROOT}/scripts/validate-wikilinks.sh" \
  --project-path "{project_path}" --json 2>/dev/null | jq -r '.broken_links // 0')

if [ "$broken_count" != "0" ]; then
  echo "ERROR: $broken_count broken wikilinks detected" >&2
  echo "  Return to Phase 12 and resolve broken links" >&2
fi
```

**IF broken links detected OR research report missing:** STOP, return to Phase 12.

---

## Step 0.5: Initialize Phase 13 TodoWrite

Add step-level todos for Phase 13:

```markdown
USE: TodoWrite tool
ADD (in addition to phase-level todos):
- Phase 13, Step 1: Validate README.md at project root [in_progress]
- Phase 13, Step 1.5: Validate insight-summary.md (conditional on arc_id) [pending]
- Phase 13, Step 2: Generate compliance report [pending]
- Phase 13, Step 3: Update sprint log (synthesis_complete = true) [pending]
- Phase 13, Step 4: Report completion summary [pending]

As you complete each step, mark the corresponding todo as completed.
```

---

## Step 1: Validate README.md at Project Root

```bash
# Verify README.md exists at project root (not in synthesis directory)
if [ -f "{project_path}/README.md" ]; then
  echo "✓ README.md found at project root"
else
  echo "WARNING: README.md not found at project root"
  echo "  Expected: {project_path}/README.md"
  echo "  Check synthesis-hub Phase 10 execution"
fi
```

**Mark Step 1 todo as completed** before proceeding to Step 1.5.

---

## Step 1.5: Validate insight-summary.md (Conditional)

If the project was configured with a story arc (`arc_id`), validate that `insight-summary.md` was generated by Phase 10.5 (cogni-narrative delegation).

```bash
# Check if arc was configured (insight-summary expected)
arc_id=$(jq -r '.arc_id // ""' "{project_path}/.metadata/sprint-log.json")
if [ -n "${arc_id}" ]; then
  if [ -f "{project_path}/insight-summary.md" ]; then
    echo "✓ insight-summary.md found (arc: ${arc_id})"
  else
    echo "WARNING: arc_id=${arc_id} was set but insight-summary.md not found"
    echo "  Phase 10.5 (cogni-narrative) may have failed"
  fi
else
  echo "INFO: No arc_id configured — insight-summary.md not expected"
fi
```

**Mark Step 1.5 validation complete** before proceeding to Step 2.

---

## Step 2: Generate Compliance Report

Generate compliance report from validation tracking:

```text
Agent Response Compliance:
- Total agents invoked: {total_agents_invoked}
- JSON-only responses: {total - verbose_agents} ({compliance_rate}%)
- Verbose responses: {verbose_agents} ({100 - compliance_rate}%)
```

**Mark Step 2 todo as completed** before proceeding to Step 3.

---

## Step 3: Update Sprint Log

Update `.metadata/sprint-log.json` with completion markers:

```bash
# Update sprint log with Part 3 (Synthesis) completion
jq '.synthesis_complete = true | .workflow_state = "completed"' \
  .metadata/sprint-log.json > .metadata/sprint-log.json.tmp && \
  mv .metadata/sprint-log.json.tmp .metadata/sprint-log.json
```

**Mark Step 3 todo as completed** before proceeding to Step 4.

---

## Step 4: Report Completion Summary

### Report Completion

```text
✓ Phase 13: Research complete

Project: [ProjectName]
Entities Created: {total_entities}
README: {project_path}/README.md (entry point at project root)
Synthesis: {synthesis-directory}/
Insight Summary: {project_path}/insight-summary.md (if arc_id was set)
Obsidian Vault: Open {project-path} in Obsidian to explore graph view
JSON-Only Compliance: {compliance_rate}% ({compliant_count} of {total_agents} agents)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
What's Next?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

POLISH (optional — improve writing quality before export)
  /polish-research              Parallel copywriting across all synthesis outputs
                                 (megatrends, trends, dimension syntheses, insight summary).
                                 Flags: --scope all|synthesis|insight|megatrends|trends
                                        --skip-review (skip 5-persona stakeholder review)

EXPORT (choose one or more formats)
  /export-html-report           Interactive HTML — navigation, graph view, theme support.
                                 Flags: --theme <id> (default: digital-x), --list-themes
  /export-pdf-report            Formal A4 PDF — cover page, TOC, source index.
                                 Flags: --theme <id>, --list-themes
  /export-rag                   Flat markdown files optimized for RAG / Claude Projects.
                                 Flags: --entity-types <list>, --include-report

EXPLORE
  Open the project folder in Obsidian and use Graph View to browse
  the knowledge graph. Start from README.md at the project root.
```

### Self-Verification Before Completion

**Verify all steps completed:**

1. Did you validate README.md exists at project root? ✅ YES / ❌ NO
2. Did you validate insight-summary.md (if arc_id was set)? ✅ YES / ❌ NO
3. Did you generate compliance report with agent metrics? ✅ YES / ❌ NO
4. Did you update sprint log with synthesis_complete = true? ✅ YES / ❌ NO
5. Did you report completion summary with entity counts? ✅ YES / ❌ NO

⛔ **IF ANY NO: STOP.** Return to incomplete step before proceeding.

### Mark Phase 13 Complete

- Update TodoWrite: Phase 13 → completed
- Update sprint metadata: `current_phase → "completed"`, `synthesis_complete → true`

**Mark Step 4 todo as completed** to finalize the research.

---

## Phase 13 Completion Checklist

### ⛔ MANDATORY: All items MUST be checked to confirm research completion

Before marking Phase 13 complete in TodoWrite, verify:

- [ ] Phase entry verification passed (Phase 12 complete)
- [ ] README.md validated at project root
- [ ] insight-summary.md validated (if arc_id was set)
- [ ] Compliance report generated
- [ ] Sprint log updated with synthesis_complete = true
- [ ] Completion summary reported with entity counts
- [ ] All step-level todos marked as completed
- [ ] All self-verification questions answered YES
- [ ] Phase 13 todo marked completed in TodoWrite

---

## Research Complete

After Phase 13 completion:

1. **Sprint log updated:** `synthesis_complete = true`, `workflow_state = "completed"`
2. **All entities created:** Check entity counts in completion report
3. **Obsidian ready:** Guide user to open project in Obsidian, starting with README.md
4. **Graph view:** Explore knowledge graph connections in Obsidian graph view

**Congratulations!** The deeper-research workflow is complete. The research project is ready for exploration and analysis in Obsidian.
