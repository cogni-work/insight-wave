version: "1.0.0"
script:
  name: "split-implementation-patterns.sh"
  path: "skills/dimension-planner/scripts/split-implementation-patterns.sh"
  purpose: "Split implementation-patterns.md into three focused thematic files"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
    1: "Validation error"
    2: "File operation error"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "1.0.0"
    date: "2025-11-25"
    changes: "Initial contract generation"
    breaking: false
