version: "1.1.0"
script:
  name: "unpack-dimension-plan-batch.sh"
  path: "skills/dimension-planner/scripts/unpack-dimension-plan-batch.sh"
  purpose: "Parse batched dimension plan JSON containing multiple dimensions and create all markdown files"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
    2: "Invalid arguments or missing required files/paths"
    3: "Schema validation failed"
    4: "Question count mismatch"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "1.1.0"
    date: "2025-12-31"
    changes: "Initial contract generation"
    breaking: false
