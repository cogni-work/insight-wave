version: "0.0.0"
script:
  name: "validate-question-files.sh"
  path: "skills/dimension-planner/scripts/validate-question-files.sh"
  purpose: "Validates refined question files for empty question text, placeholder values, and missing headings"
  category: "utilities"
interface:
  parameters:
  output:
    format: "json"
    schema:
      success: "boolean"
      data: {}
      error: "string (if success=false)"
  exit_codes:
    0: "Success"
compatibility:
  min_bash_version: "3.2"
  dependencies: []
  environment:
    - "CLAUDE_PLUGIN_ROOT (optional for relative paths)"
changelog:
  - version: "0.0.0"
    date: "2025-11-24"
    changes: "Initial contract generation"
    breaking: false
