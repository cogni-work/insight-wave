# SCHEMA — {{wiki_name}}

_Created: {{created_date}}_

This file is the contract for how this wiki is structured. It lives inside the wiki (not inside the plugin) so the wiki remains self-describing even if cogni-wiki is uninstalled or replaced.

## Directory layout

```
<wiki-root>/
├── SCHEMA.md                 This file — conventions and contract
├── raw/                      Immutable source documents (papers, URLs, transcripts, images, data)
├── assets/                   Attachments referenced from pages (screenshots, figures, exports)
├── wiki/
│   ├── index.md              One-line summary catalog of every page
│   ├── log.md                Append-only operation log
│   ├── overview.md           Evolving "state of the wiki" synthesis
│   ├── concepts/             type: concept
│   ├── entities/             type: entity
│   ├── summaries/            type: summary
│   ├── decisions/            type: decision
│   ├── interviews/           type: interview (incl. tag:customer-call)
│   ├── meetings/             type: meeting
│   ├── learnings/            type: learning (incl. tag:retro)
│   ├── syntheses/            type: synthesis (filed-back query answers)
│   ├── notes/                type: note
│   └── audits/               lint-YYYY-MM-DD.md / health-YYYY-MM-DD.md (R3-exempt)
└── .cogni-wiki/
    ├── config.json           Plugin metadata
    └── queue/                Persistent ingest queue (v0.0.35+, T3.1)
        ├── pending/          Job files (JSON) waiting to be picked
        ├── running/          The single in-flight job (if any)
        ├── done/             Successfully completed jobs
        └── failed/           Jobs that failed mid-ingest; retry via --queue-retry
```

The queue dir is created lazily on the first `wiki-ingest --enqueue`; wikis that never use queue mode never grow it. Queue mode is documented in `skills/wiki-ingest/SKILL.md` §"Mode D".

## Page frontmatter

Every page file begins with YAML frontmatter:

```yaml
---
id: <slug>                       # Must match filename without .md
title: <human-readable title>
type: concept | entity | summary | decision | interview | meeting | learning | synthesis | note | source | question
tags: [tag1, tag2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources:                         # Optional — relative paths or URLs
  - ../raw/paper-xyz.pdf
  - https://example.com/article
---
```

The `type:` value MUST match the directory the page lives in (e.g. `type: concept` lives at `wiki/concepts/<slug>.md`). `wiki-health` reports `type_directory_mismatch` when a hand-edit drifts the two apart.

### Types

- **concept** — an idea, framework, or model
- **entity** — a person, organization, product, or place
- **summary** — a condensed version of a raw source
- **decision** — a choice made and the reasoning behind it
- **interview** — captured conversation (set `tag:customer-call` for a sales/CS variant)
- **meeting** — meeting notes
- **learning** — a generalized takeaway (set `tag:retro` for a retrospective variant)
- **synthesis** — an LLM-derived answer filed back into the wiki by `wiki-query --file-back yes`; cites its wiki provenance via `wiki://<slug>` entries in `sources:`
- **note** — a loose observation that hasn't crystallized into a concept yet
- **source** — an ingested source body. Written by `cogni-knowledge:knowledge-ingest` (or any future generic ingestor) as the substrate downstream writers compose from. Per-type semantics (e.g. `pre_extracted_claims:` frontmatter) are owned by the ingestor; cogni-wiki only recognises the type and routes the page to `wiki/sources/`
- **question** — a research-question node — one per sub-question of a research run. Written by `cogni-knowledge:knowledge-ingest`; its body `[[links]]` the source findings that answer it, and each answering source links back, so the question→finding relation joins the backlink graph. Per-type semantics (`theme_label:`, `sub_question_id:`, `## Findings` body) are owned by the ingestor; cogni-wiki only recognises the type and routes the page to `wiki/questions/`

## Linking

- Use `[[page-slug]]` for links to other wiki pages — slug-only, no path. The script layer resolves the slug to its per-type directory.
- Use standard markdown links for external URLs and raw sources: `[label](../raw/file.pdf)`
- Every `[[link]]` should be backed by an actual file under one of the per-type directories or `wiki/audits/`; the `wiki-lint` skill reports broken links

## Forward → reverse link contract

Wikilinks are bidirectional. Every forward `[[link]]` from page A to page B implies a documented reverse action on page B. The contract below is the single source of truth — `wiki-ingest`'s `backlink_audit.py` proposes candidates against it, `wiki-lint`'s `reverse_link_missing` check validates it, and a human editing the wiki without Claude (the "5-year hand-off" promise) follows it by hand.

| Rule ID | Forward action (page A) | Required reverse action (page B) | Audited by |
|---------|------------------------|---------------------------------|------------|
| `R1_bidirectional_wikilink` | A's body contains `[[B]]` | B's body should contain `[[A]]` somewhere — typically as a one-line natural-language reference, not a dump | `backlink_audit.py` proposes; `wiki-lint reverse_link_missing` validates |
| `R2_synthesis_wiki_source` | A is `type: synthesis` and lists `wiki://B` in `sources:` | **No reverse link required** — synthesis pages are leaves on the citation graph; forcing every cited page to link back would flood high-traffic concepts with synthesis backlinks | `wiki-lint synthesis_no_wiki_source` (forward direction only) |
| `R3_audit_report` | Any page contains `[[lint-YYYY-MM-DD]]` or `[[health-YYYY-MM-DD]]` | **No reverse link required** — lint and health reports are terminal, append-only, and never edited | n/a (audit reports are exempt from `reverse_link_missing` on both ends) |

Notes:

- All current `type:` values (concept, entity, summary, decision, interview, meeting, learning, synthesis, note, source, question) participate in `R1` symmetrically. There are no per-type asymmetries today; the per-type page directory layout (v0.0.28+, schema_version `0.0.5`) is purely a filesystem reorganisation — every slug remains globally unique and every `[[wikilink]]` continues to address pages by slug alone. The `source` type (schema_version `0.0.6`, v0.0.44+) and the `question` type (schema_version `0.0.7`, v0.0.50+) are additive extensions under the same layout rule — pages live at `wiki/sources/<slug>.md` / `wiki/questions/<slug>.md` and participate in `R1` like every other type.
- A backlink should read like prose, not a dumped list. The reverse `[[A]]` typically lives next to a sentence that earns its presence on B — see `wiki-update`'s diff-before-write discipline.
- `R3` exempts pages whose filename starts with `lint-` or `health-` on both ends. Such reports live under `wiki/audits/` (v0.0.28+). As of v0.0.27, `wiki-health` only writes a log line and does not yet create `health-*.md` page files; the exemption + directory are forward-compatible plumbing for a future health-report-as-page feature.
- `wiki-lint reverse_link_missing` is a warning, not an error: it surfaces missing reverses but never auto-fixes them, mirroring the "lint reports, never auto-fixes" rule. Apply via `wiki-update` or, for fresh ingests, via `wiki-ingest`'s backlink-audit step.

## Log format

Append only. Format:

```
## [YYYY-MM-DD] {ingest|query|synthesis|lint|health|update|setup|migrate|prefill|graph|queue|compose|verify|finalize} | one-line note
```

The `synthesis` prefix (v0.0.23+) marks filed-back query answers — distinct from un-filed `query` reads. The `health` prefix (v0.0.27+) marks zero-LLM structural pre-flights — distinct from tokenful `lint` runs. The `migrate` prefix (v0.0.28+) marks one-shot layout migrations (e.g. flat `wiki/pages/` → per-type dirs). The `prefill` prefix (v0.0.33+) marks foundation-page seeding from the plugin's curated `foundations/` library — distinct from regular `ingest` because the source is a plugin-shipped concept stub, not a user-supplied raw file. The `graph` prefix (v0.0.34+) marks `wiki-dashboard --graph` runs (Pass 1 deterministic + Pass 2 LLM-inferred). The `queue` prefix (v0.0.35+) marks persistent-queue events from `wiki-ingest`'s Mode D — `enqueued <id>`, `failed <id>`, `retried <id>`. All six are surfaced separately by `wiki-resume` and `wiki-dashboard`. Never rewrite or reorder log entries. The `queue` extension is purely additive: pre-v0.0.35 readers count `queue` lines in their catch-all bucket without crashing, so no `schema_version` bump is needed.

The `compose`, `verify`, and `finalize` prefixes (v0.0.45+) mark the write-side phases of `cogni-knowledge`'s v0.1.0 inverted pipeline when it runs against a bound wiki — `compose` is the draft step (`knowledge-compose`), `verify` is the zero-network claim-alignment step (`knowledge-verify`), and `finalize` is the synthesis-page deposit (`knowledge-finalize`, which also writes a `type: synthesis` page like `wiki-query --file-back`). They are cogni-knowledge operations, not cogni-wiki ones — documented here for the same reason the `type: source` page type is (an external write-side consumer authors them against this wiki, so the enum that catalogs `wiki/log.md` must recognise them). This extension is purely additive, same posture as `queue`: pre-v0.0.45 readers bucket the three prefixes in their catch-all without crashing, so no `schema_version` bump is needed. `knowledge-ingest` already reuses the existing `ingest` prefix; `knowledge-plan` / `knowledge-curate` / `knowledge-fetch` never touch `wiki/log.md` (they write only the project's `.metadata/` and the fetch-cache), so they get no prefix.

## Index format

`wiki/index.md` lists every page as:

```
- [[page-slug]] — one-sentence summary
```

Grouped under `##` category headings. The `wiki-ingest` skill maintains this file.

## Golden rules

1. **Claude writes the wiki; the user curates the raw sources.**
2. **Every query reads the wiki — never answers from memory.**
3. **Diff before write** — page updates show the planned change before modifying a file.
4. **Citations required** — any new claim in a page links to a source in `raw/` or an external URL.
5. **Append-only log** — operations are recorded, never rewritten.
