# Design Variables Pattern (moved)

This convention now lives with the theme lifecycle in cogni-publishing:
`cogni-publishing/references/design-variables-pattern.md`.

This file is a pointer, not an authority, and holds no rules of its own. It
stays only so existing readers of the old path land on the canonical copy
until they are repointed. The saved-theme contract behind the pattern is
`cogni-publishing/references/theme-artifact-contract.md`.
