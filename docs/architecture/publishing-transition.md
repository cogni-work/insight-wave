# Publishing transition: ownership and consumer inventory

Status: decided architecture baseline for roadmap issues #1943–#1951. This record is an inventory and migration contract; it changes no runtime behavior.

## Decision

`cogni-publishing` will own the source-to-deliverable publishing lifecycle: versioned brief contracts, theme lifecycle, semantic composition, HTML/PPTX rendering and verification, and the editorial narrative/copy subsystem. `cogni-workspace` will remain the horizontal owner of shared workspace state and operations: preferences and generated settings, language delivery, plugin and optional-tool discovery, MCP installation, health/diagnostics, markets, claims, project discovery, and Obsidian integration.

The transition has three hard boundaries:

1. Narrative and copywriting move together. `text-to-narrative` calls copywriter readability, while copywriter reads narrative arc and language contracts at runtime; splitting them would create a private cross-plugin cycle.
2. Theme authoring, import, validation, discovery, selection, bundled resources, and semantic-token compilation have one lifecycle owner: `cogni-publishing`. Existing saved themes remain readable in place; consuming a selected artifact does not make the consumer an authoring owner.
3. Claims, supported markets, project discovery, workspace preferences, plugin/tool setup, diagnostics, and Obsidian remain in `cogni-workspace`. They have live non-publishing consumers and no landed replacement. Age or host overlap is not evidence for removal.

No row below is provisional. “Retain” means supported by `cogni-workspace`; “move” means one canonical implementation in `cogni-publishing` with a bounded compatibility route; “remove” requires both a redundancy witness and a remaining-consumer countercheck.

## Inventory schema

Every row records the current owner and source witness, concrete consumers, interface visibility, inputs and outputs, final disposition, destination, implementation child, and a measurable witness or exit condition. Paths and line references describe the `origin/main` tree inspected for #1942. Bounded source and destination path scopes for every move or contract-migration row are given in the path map below the compatibility-route index.

| ID | Responsibility and current owner / source witness | Concrete consumers | Interface; inputs → outputs | Disposition and destination | Later child | Verification witness or exit condition |
|---|---|---|---|---|---|---|
| E1 | Narrative composition and readability measurement — `cogni-workspace`; `cogni-workspace/skills/text-to-narrative/SKILL.md:21-29,255-262` | portfolio pitch/customer narratives, sales presentations, trends reports, consult action-field routes; marketing next-action dispatches (`cogni-marketing/skills/abm/SKILL.md:124`, `cogni-marketing/skills/thought-leadership/SKILL.md:143`, `cogni-marketing/references/data-model.md:184`, `cogni-marketing/README.md:190`) | Public skill plus private bundled references/scripts; source markdown, arc/target options and citations → arc narrative, QA envelope and frozen design brief | **Move as editorial group E** to `cogni-publishing`; no independent workspace implementation | #1949; callers switch in #1950 | Publishing-only fixture runs narrative and readability with workspace absent; old skill route delegates; repository search finds no active sibling-private path after #1950 |
| E2 | Arc-aware copywriting, translation and review — `cogni-workspace`; `cogni-workspace/skills/copywriter/SKILL.md:1-44,172-181` | portfolio, sales and trends optional polish; consult optional polish (`cogni-consult/skills/consult-publish/SKILL.md:100-103,162`, `cogni-consult/references/publish-routing.md:176-196`, `cogni-consult/skills/consult-design-thinking/SKILL.md:443-463`); marketing polish dispatches (`cogni-marketing/skills/demand-generation/SKILL.md:129`, `cogni-marketing/skills/lead-generation/SKILL.md:131`, `cogni-marketing/skills/thought-leadership/SKILL.md:142`, `cogni-marketing/CLAUDE.md:70`, `cogni-marketing/references/data-model.md:183`); E1 readability assets | Public skill plus private agent/references/scripts; document, scope, language, persona and `arc_id` → in-place polished/translated document and review result | **Move in the same editorial group E** as E1; shared arcs, language rules and readability assets become publishing-owned | #1949; callers switch in #1950 | Publishing-only test exercises both directions: E1→readability and E2→arc contracts; no mirrored arc assets or renderer dependency |
| E3 | Narrative arc registry/contracts, validation, density ceilings and presentation intent — `cogni-workspace`; `cogni-workspace/CLAUDE.md:203-211,218` | E1/E2; portfolio templates; Corporate Visions sales flow; trends smarter-service composer; website arc taxonomy; consult presentation-intent copy | Private paths currently used as cross-plugin contracts; `arc_id`, evidence and target → headings, composition/validation rules, visual intent and brief grammar | **Move with group E** and expose versioned public publishing contracts; preserve byte-level headings, inputs and semantics | #1943 defines public artifact/version rules; #1949 moves assets; #1950 migrates consumers | All named consumers resolve a publishing public contract without locating `cogni-workspace/skills/**`; parity fixtures preserve canonical content |
| E4 | Narrative-derived design-brief construction — `cogni-workspace`; `cogni-workspace/skills/text-to-narrative/SKILL.md:296-327`, governed by the canonical brief contract at `cogni-workspace/skills/text-to-narrative/references/design-brief-template.md:1-10` | Claude Design handoff today; future publishing composition/renderers | Public skill output contract; validated narrative, target and density limits → ordered units, frozen copy, talk track, sources, evidence state and visual intent | **Move with group E**; normalize the existing brief without rewriting text, notes, sources, evidence or order. The template's three semantics are preserved verbatim: the brief stays self-contained, every line remains a verbatim selection from the narrative (frozen copy), and the ceilings applied stay written into the brief's own `density.ceilings` | #1943 normalization contract, #1945 composition, #1946/#1947 renderers, #1948 proof, #1949 ownership move | Captured brief field comparison is lossless; content fingerprint is unchanged through composition and both renderers |
| P1 | Consult-native brief production — `cogni-consult`; `cogni-consult/skills/consult-publish/SKILL.md:118-163` and `cogni-consult/references/publish-routing.md:42-85,136-196` | consult deliverables in slides, web-poster, report and infographic formats | Public consult skill and explicit brief contract; framework-shaped deliverable plus elected format → native outline/report/infographic brief and `publish[]` path provenance | **Retain in `cogni-consult`** as a direct publishing input. Never force `text-to-narrative`; optional polish remains separable | #1943 adds direct-brief input; #1950 adds only supported elected render routes | Consult route tests reject narrative dispatch; source path provenance and framework order survive; unsupported formats keep their existing handoff |
| T1 | Complete theme lifecycle: author/import, validate, discover/select, store, compile tokens, bundled themes — `cogni-workspace`; `cogni-workspace/CLAUDE.md:10-26` | Visual: portfolio-dashboard, website-build and website-setup, pinned as SKILL.md theme-contract references (not renders) by Phase C of `cogni-workspace/scripts/verify-theme-backcompat.sh:400-438`; trends-dashboard, consult-dashboard and workspace-dashboard read the same contract but are not in the harness's `VISUAL_CONSUMERS` list. Voice: **no live consumer outside workspace**. Phase D (`verify-theme-backcompat.sh:441-477`) only asserts that every `theme.md` carries the `## Voice & Copy Guidelines` header (tbc21) and that the `cogni-sales/` directory exists (tbc22); a scan of `cogni-sales/**` finds no `theme.md`, `theme_path` or voice-section read, so the harness's `VOICE_PLUGINS` entry is a stale name, not a consumer witness. The section's actual readers are the importer's stub policy (`cogni-workspace/scripts/import-claude-design-bundle.py:48-56`) and the presets suite (`cogni-workspace/tests/test-bundled-presets.sh:177-180`), both inside T1 | Public `manage-themes` plus private scripts/schemas/references/artifacts; bundle/local theme/explicit path → canonical saved theme, `theme_path`, `theme_name`, `theme_slug`, tiers and tokens | **Move the entire lifecycle** to `cogni-publishing`; one canonical representation and implementation. Workspace may delegate but cannot remain a storage/authoring authority | #1944; consumers switch in #1950; residue removed in #1951 | Publishing-only discovery reads bundled, user, tier-0 and tiered themes; alias, re-import and backcompat suites pass; workspace entry contains delegation only |
| T2 | Saved theme and design-variable consumption — selected artifacts currently originate in workspace; `cogni-workspace/CLAUDE.md:30-45` | portfolio-dashboard, trends-dashboard, consult-dashboard, workspace-dashboard and website-build/setup; there is no live voice consumer (see T1) | Public artifact contract; selected theme or `design-variables.json` → consumer-owned CSS/HTML behavior | **Migrate contract ownership** to publishing, but retain read-only consumption in each business plugin and preserve existing user theme bytes/location | #1943 contract, #1944 lifecycle, #1950 caller paths | `theme_path`/`theme_name`/`theme_slug` and explicit-path precedence pass unchanged; saved artifacts work without invoking authoring or initializing workspace |
| W1 | Workspace preferences and generated settings — `cogni-workspace`; `cogni-workspace/skills/manage-workspace/SKILL.md:67-89` | every installed plugin resolving language, plugin roots and optional shared Python environment | Public workspace skill with private generator; user choices/plugin inventory → `.workspace-config.json`, `.claude/settings.local.json`, `.workspace-env.sh` and `_ROOT`/`_PLUGIN` variables | **Retain in `cogni-workspace`**. Publishing may read optional preferences after explicit inputs, never require them | #1943 defines supplied→project→optional-workspace→bundled precedence; #1951 confirms retained scope | Publishing standalone fixtures pass with workspace files/env absent; explicit values beat preferences; workspace generation tests remain green |
| W2 | Language delivery and user-facing output guidance — `cogni-workspace`; `cogni-workspace/CLAUDE.md:60-94`, `cogni-workspace/hooks/hooks.json:1-20` | main-session users and vertical-plugin overlays; copywriter consumes fuller language rules | Host setting plus public register and SessionStart hook; workspace language and overlay → host `# Language`, orthography context and wording guidance | **Retain in workspace**; move only copywriter-owned editorial language assets with E2. Do not centralize plugin-owned output styles | #1949 separates editorial assets; #1951 reconciles guidance | Hook still emits valid `additionalContext`; vertical overlays resolve register fail-soft; publishing works without the hook; no duplicate canonical language authority |
| W3 | Plugin discovery, registry and path wiring — `cogni-workspace`; `cogni-workspace/skills/manage-workspace/SKILL.md:55-61,73-89,227-256` | all cross-plugin calls, updates and workspace dashboard | Public workspace skill/private discovery and settings scripts; marketplace cache and confirmed plugin list → versions, paths, generated env vars and registry state | **Retain in workspace**; host marketplace discovery is an input, not a replacement for confirmed workspace wiring | #1951 confirms retained scope and removes only publishing-specific assumptions | Add/remove plugin fixture updates generated vars; countercheck: consumers still use `_PLUGIN`/fallback ladders, so no removal until those reads are gone |
| W4 | Optional Python/tool dependency provisioning — `cogni-workspace`; `cogni-workspace/skills/manage-workspace/SKILL.md:93-110,260-276` | plugins with registered optional fallbacks | Public workspace workflow/private registry and installer; installed plugin set → shared venv and dependency status | **Retain in workspace**; publishing renderer runtime is independently pinned and not provisioned during ordinary validation | #1943 establishes renderer boundary; #1951 audits setup wording | Publishing validator passes without Node/model/render package; workspace optional-dependency checks still identify `required_by` consumers |
| W5 | MCP install, native detection and host config patching — `cogni-workspace`; `cogni-workspace/CLAUDE.md:96-106` | portfolio Excalidraw, website Pencil/browser paths, workspace browser automation and diagnostics | Public `install-mcp`, private registry/installer/config patcher; required plugin set, repo/native app and target host → installed wrapper plus Desktop/CLI config entry and restart advice | **Retain in workspace**. Publishing may consume tools but does not own machine-wide installation. Remove only stale publishing-specific requirements if later proven unused | #1951 | Exact host capability countercheck: hosts load configured MCPs but do not clone/build wrappers or write both configs. Registry-required consumers and status probes remain passing |
| W6 | Supported-market taxonomy and overlay join — `cogni-workspace`; `cogni-workspace/scripts/get-market-config.py:1-29,39-52` and `cogni-workspace/references/supported-markets-registry.json` | trends overlay (`cogni-trends/CLAUDE.md:149`); portfolio agent and validator reads (`cogni-portfolio/agents/market-researcher.md:31`, `cogni-portfolio/agents/customer-researcher.md:30`, `cogni-portfolio/agents/competitor-researcher.md:25`, `cogni-portfolio/skills/customers/SKILL.md:142`, `cogni-portfolio/scripts/validate-entities.sh:360-367`); knowledge orchestrators and curator (`cogni-knowledge/skills/knowledge-curate/SKILL.md:54-72`, `cogni-knowledge/skills/knowledge-plan/SKILL.md:96,137`, `cogni-knowledge/skills/knowledge-setup/SKILL.md:116`, `cogni-knowledge/agents/source-curator.md:59-80`, pinned by `cogni-knowledge/tests/test_skill_contracts.sh:98-100,160-162` and `cogni-knowledge/tests/test_language_config_contract.sh:112`); market-aware documentation and dashboards | Public JSON registry/private merge utility; market code, plugin and overlay → standard JSON envelope with shared fields plus operational metadata | **Retain in workspace**; unrelated to publishing and no replacement exists | #1951 explicitly preserves it | Registry/overlay tests pass; every overlay key resolves; removal is forbidden absent a named replacement owner, migrated callers and equivalent envelope witness |
| W7 | Shared project discovery — `cogni-workspace`; `cogni-workspace/scripts/discover-plugin-projects.sh:1-35` | thin wrappers in `cogni-consult/scripts/discover-projects.sh:1-24`, `cogni-portfolio/scripts/discover-projects.sh:1-25`, `cogni-trends/scripts/discover-projects.sh:1-28` | Private generic called by plugin wrappers; root/registry/extractor/find specs → `{count, search_root, projects[]}` | **Retain in workspace**; no publishing destination or replacement | #1951 explicitly preserves it | All three wrappers pass in installed-cache and monorepo shapes; remove only after a named owner and migrated-wrapper fixture exist |
| W8 | Claim submission, verification and correction state — `cogni-workspace`; `cogni-workspace/CLAUDE.md:171-184` | trends and portfolio pipelines, consult assumptions, opt-in knowledge resweep | Public `claims` skill plus private agents/scripts; sourced assertions and entity refs → verdicts, deviations, resolutions and `{working_dir}/cogni-claims/**` state | **Retain in workspace**. The on-disk `cogni-claims/` name is permanent compatibility, not temporary debt | #1951 explicitly preserves it | Representative consumer round-trips pass; user-data paths remain byte-compatible; removal requires a concrete replacement and safe migration witness, neither exists |
| W9 | Workspace health, plugin diagnostics and configuration dashboard — `cogni-workspace`; `cogni-workspace/CLAUDE.md:137-148`, `cogni-workspace/skills/workspace-status/SKILL.md:209-265,330-352`, `cogni-workspace/skills/workspace-dashboard/SKILL.md:19-23,80-109` | workspace operators and troubleshooting/help route | Public skills/command plus private probes/generator; workspace files, plugins, themes, dependencies, MCPs, markets and hooks → actionable text diagnosis or self-contained HTML | **Retain in workspace**; update theme/publishing labels after caller migration, do not move operational diagnosis into publishing | #1951 | Health runs with the post-migration dependency graph and gives one valid next action; dashboard inventory matches manifests and no longer claims workspace publishing ownership |
| W10 | Obsidian vault integration and shared note-frontmatter convention — `cogni-workspace`; `cogni-workspace/CLAUDE.md:120-127` | users launching plugin work from Obsidian and plugins emitting browsable markdown | Public workspace subflow/private portable scripts/templates; workspace path and user choice → `.obsidian/` config, terminal profiles and preserved user customization | **Retain in workspace**; unrelated to publishing | #1951 confirms retained scope | Init/update fixtures preserve existing customization; no publishing install depends on Obsidian |
| W11 | Issue reporting and ecosystem diagnostics — `cogni-workspace`; `cogni-workspace/CLAUDE.md:137-148` | every marketplace plugin/user reporting a problem | Public `cogni-issues`, `workspace-status` and `/troubleshoot`; symptom/repo/plugin → structured issue or diagnosis | **Retain in workspace**; no publishing responsibility | #1951 confirms retained scope | Issue resolver still maps plugin ownership and diagnostics still cover post-migration dependencies |
| W12 | Output style — `cogni-workspace`; `cogni-workspace/CLAUDE.md:71-80,94` | users who explicitly select workspace-advisor | Host-discovered public style file; opt-in selection → session stance | **Retain plugin-owned**; host discovery is the runtime, not a replacement for the style. Never copy or force globally | #1951 only reconciles guidance | Style remains root-discoverable, opt-in and `keep-coding-instructions: true`; no generated workspace copy exists |
| R1 | Local brief renderer and retired story-to-* compatibility — formerly workspace, already removed; `cogni-workspace/CLAUDE.md:213-220` | Countercheck found no in-repo producer; six surviving libraries are consumed by E1, portfolio, website and consult | No live public route; historical triggers and deleted implementation → none | **Remove with evidence**, already complete. Retain the six measured libraries until their consumers migrate; do not infer those files are renderer residue | #1943-#1948 create a new contract-first renderer; #1949/#1950 move consumers; #1951 cleans only proven residue | Retired-trigger ledger stays non-routable; repository search finds no live old renderer dispatch; each surviving library has a named consumer until its canonical destination lands |
| M1 | Marketplace/plugin descriptions and user guidance — current workspace ownership claim at `.claude-plugin/marketplace.json:54-65` and `cogni-workspace/.claude-plugin/plugin.json:1-24` | installer/update detection, users choosing skills, generated ecosystem docs/help | Public metadata and documentation; live tree/capabilities → discoverability and dependency guidance | **Migrate wording, not version values**, only after canonical moves and caller migration; publishing gains its own entry in #1943 | #1943 creates publishing entry; #1950 updates caller guidance; #1951 reconciles workspace/help/docs | Version-bump gate reports no feature-branch version edit; descriptions, diagrams and dependency tables agree with the final tree |

## Coverage by evidence surface

The inventory is organized by responsibility, so this index proves that non-code consumer shapes were not silently skipped.

| Required evidence surface | Inventory rows | Primary witnesses |
|---|---|---|
| Executable calls and skill dispatches | E1, E2, P1, T1, W5, W6, W8, W9 | `text-to-narrative`, `copywriter`, `consult-publish`, `manage-themes`, `install-mcp`, `get-market-config.py`, `claims`, `workspace-status` |
| Reference/private-path reads | E2, E3, P1, T2, W2, W6, W7 | arc/language contracts, presentation intent, design variables, output register, market overlays, discovery wrappers |
| Configuration and environment reads/writes | W1, W3, W4, W5 | `.workspace-config.json`, settings, env exports, venv registry and host MCP configs |
| Theme artifacts and saved outputs | T1, T2 | bundled/user themes, manifests, semantic tokens, `theme.md`, `design-variables.json` |
| Hooks | W2 | `hooks/hooks.json` and `on-session-start-language.sh` |
| Tests and guards | E1-E4, T1-T2, W2-W9, R1 | narrative/brief/arc guards, theme backcompat, market/discovery/claims/setup/diagnostic suites and retired-trigger ledger |
| Marketplace/plugin entries | M1 | root marketplace and workspace manifest |
| User-facing guidance | P1, W2, W9, W11, W12, M1 | consult route, output register, status/dashboard/troubleshoot, style, READMEs and dependency tables |

## Cohesive editorial transfer contract

Rows E1–E4 are one migration unit. The boundary preserves both dependency directions:

- `text-to-narrative` invokes `skills/copywriter/scripts/readability.sh` and reads the copywriter readability band (`cogni-workspace/skills/text-to-narrative/SKILL.md:27-29,255-258`).
- Copywriter arc mode reads the narrative registry, arc contract, techniques and shared language headings at runtime (`cogni-workspace/skills/copywriter/SKILL.md:172-181`).
- The narrative output remains four ordered arc sections plus sources; the design brief remains a target-specific selection whose copy, numbers, citations, evidence status, notes and order are frozen (`cogni-workspace/skills/text-to-narrative/SKILL.md:262,296-327`).
- Consult-native briefs remain a separate direct input. They preserve Pyramid/SCQA/MECE structure and may carry presentation intent, but never require re-narration (`cogni-consult/references/publish-routing.md:44-85`).

The public replacement must therefore make the editorial assets locally resolvable inside publishing and separately normalize both the existing narrative brief and a structured direct brief. Rendering begins only after normalization/freezing; copywriter is never used after the freeze boundary to make content fit.

## Theme ownership contract

Publishing becomes the single lifecycle owner for theme authoring/import, validation, discovery/selection, saved theme representation, bundled resources, token compilation and compatibility projections. User-owned themes are not moved or overwritten. Publishing reads their optional configured location and returns the existing `theme_path`, `theme_name` and `theme_slug` handoff.

Business plugins continue to own their `design-variables.json` outputs and rendering use. That is artifact consumption, not a second theme lifecycle. During transition the workspace entry point may delegate to publishing; it must not keep a forked validator, importer, catalog or canonical theme store.

## Host-overlap decisions

| Workspace surface | Exact host capability | Affected consumers and counterexample | Decision |
|---|---|---|---|
| Language setting | Claude Code turns `language` in settings into a `# Language` prompt section | Main sessions consume it, but the host does not generate workspace/plugin settings or supply the German orthography override; subagents still need plugin-local hooks | Retain W1/W2; host support replaces neither generator nor orthography contract |
| Output style | Host discovers root `output-styles/` and applies an explicitly selected style | Users consume `workspace-advisor`; host provides discovery, not its content, and global force would affect unrelated sessions | Retain W12 as opt-in plugin content |
| Marketplace/plugin discovery | Host installs and exposes marketplace plugins | Cross-plugin callers still need confirmed paths, environment variables and non-host shell exports | Retain W3 until those concrete consumers disappear |
| MCP loading | Host loads configured MCP servers and exposes tools | Portfolio/website/workspace need installed binaries/apps and correct Desktop/CLI config; host loading does not clone/build wrappers or patch both configs | Retain W5; remove only a registry row whose named consumers are gone |
| Browser/visual authoring | Host/Claude Design can render or expose visual tools | This obsoleted the producerless old local render chain, but not themes, MCP setup, dashboards or selected-artifact readers | R1 remains removed; migrate T1/T2 and build new explicit renderer contracts instead of deleting adjacent infrastructure |

No additional host-overlap removal is authorized by this record.

## Compatibility-route index

“Temporary” appears only where a canonical replacement and observable exit both exist. A route without a safe exit is explicitly retained.

| Current route | Current callers | Canonical target | Compatibility action | Measurable exit condition | Child |
|---|---|---|---|---|---|
| `cogni-workspace:manage-themes` and `/manage-themes` | dashboard and website skills; users | `cogni-publishing` theme capability | Thin delegation preserving arguments/result fields and actionable missing-plugin behavior | #1950 consumer scan has no direct workspace theme dispatch and migration guidance has shipped | #1944, #1950, #1951 |
| Workspace `themes/`, tier-0 `theme.md`, tiered manifests/tokens and explicit user paths | all theme consumers | Publishing-owned canonical theme representation plus optional configured user-theme location | Read in place; generate compatibility projections; never silently move/overwrite user files | Backcompat suite passes from publishing-only install and every protected user fixture is byte-identical | #1944 |
| `theme_path` / `theme_name` / `theme_slug` and consumer `design-variables.json` | portfolio, trends, consult, workspace dashboards; website; no voice caller exists (T1) | Public publishing artifact contract | Keep field names and precedence; consumers may reuse saved design variables without invoking authoring | All inventoried consumers use public contract or documented saved-artifact path; old private reference scan is empty | #1943, #1944, #1950 |
| `cogni-workspace:text-to-narrative`, `/text-to-narrative` | portfolio (`cogni-portfolio/skills/portfolio-communicate/SKILL.md:74-75`), trends (`cogni-trends/skills/trends-resume/SKILL.md:217-227`, `cogni-trends/skills/verify-trend-report/SKILL.md:368`, the executable next-action emitter `cogni-trends/scripts/project-status.sh:867` and its suite `cogni-trends/tests/test-project-status.sh:220`), consult (`cogni-consult/skills/consult-action-fields/SKILL.md:122-136,202`; the no-re-narration guard at `cogni-consult/skills/consult-publish/SKILL.md:133` names the token without dispatching it), marketing (`cogni-marketing/skills/abm/SKILL.md:124`, `cogni-marketing/skills/thought-leadership/SKILL.md:143`, `cogni-marketing/references/data-model.md:184`, `cogni-marketing/README.md:190`); sales consumes only the arc contract (E3), not the dispatch | Publishing editorial capability | Thin old-name delegation preserving all modes and absent-plugin behavior | #1950 migrates every dispatch on the E1/E4 consumer-update paths of the path map, marketing included; exit is a scan over those paths returning no `cogni-workspace:text-to-narrative` or `/text-to-narrative` token, plus the migration window declared in #1951 | #1949, #1950, #1951 |
| `cogni-workspace:copywriter`, `/copywrite` | portfolio (`cogni-portfolio/skills/portfolio-communicate/SKILL.md:360-393`), sales (`cogni-sales/skills/why-change/SKILL.md:393,418,438`), trends (`cogni-trends/skills/verify-trend-report/SKILL.md:29-35,361-368`, `cogni-trends/skills/trends-resume/SKILL.md:211`, the executable next-action emitter `cogni-trends/scripts/project-status.sh:302-311,750,865` and its suite `cogni-trends/tests/test-project-status.sh:13-21`), consult (`cogni-consult/skills/consult-publish/SKILL.md:100-103,162,201-214,241`, `cogni-consult/references/publish-routing.md:176-196`, `cogni-consult/skills/consult-design-thinking/SKILL.md:443-463`), marketing (`cogni-marketing/skills/demand-generation/SKILL.md:129`, `cogni-marketing/skills/lead-generation/SKILL.md:131`, `cogni-marketing/skills/thought-leadership/SKILL.md:142`, `cogni-marketing/CLAUDE.md:70`, `cogni-marketing/references/data-model.md:183`) and ad-hoc users | Publishing editorial capability | Thin delegation; preserve independent polish/translate/review with renderer absent | #1950 migrates every dispatch on the E2 consumer-update paths of the path map, marketing and the consult-publish polish included; exit is a scan over those paths returning no `cogni-workspace:copywriter` token or `/copywrite` next action, plus the documented migration window | #1949, #1950, #1951 |
| Private `cogni-workspace/skills/text-to-narrative/references/**` arc/language paths | portfolio, sales, trends, website, copywriter | Versioned public publishing editorial-contract location | Provide compatibility path/delegation during caller migration; no mirror authority | All named paths resolve through publishing public contract and repository scan finds no unexplained private read | #1943, #1949, #1950 |
| `cogni-workspace/libraries/presentation-intent.md` synchronized into consult | consult publish and narrative brief | Versioned publishing direct/narrative brief contract | Preserve consult's self-contained copy until public contract is available; then point parity at publishing | Publishing-only consult fixture validates native brief and parity guard has no workspace-private source | #1943, #1950 |
| Workspace preference read by publishing | future publishing entry points | Explicit input → publishing project config → optional workspace preference → bundled default | Optional, fail-soft read only; never workspace initialization requirement | Standalone publishing suite passes with all workspace files/env absent and precedence fixtures pass | #1943 |
| Marketplace description claiming workspace owns publishing | install/update UI, docs/help | Separate publishing entry plus reduced workspace description | Update wording only after implementation/callers match; feature branches do not bump versions | Marketplace, plugin manifests, diagrams, help and README dependency tables agree with final tree | #1943, #1951 |
| Manifest aliases | None: `rg -n '\"aliases?\"' .claude-plugin cogni-*/.claude-plugin --glob '*.json'` returned no alias key on the #1942 base | No compatibility route exists | Keep the class explicit in this inventory; if #1943 introduces an alias, that child must add its callers, canonical target and removal condition here before #1951 | No alias may be removed or called temporary without a later search witness and migrated caller set | #1943, #1951 |
| `{working_dir}/cogni-claims/` data path | claims skill, trends, portfolio, consult, knowledge resweep | Same path and workspace claims owner | **Retained compatibility with no exit**; user data cannot be found/migrated safely across every workspace | Permanent support tests pass; no “temporary” or removal promise | #1951 |
| Project wrapper resolution ladders (env → installed cache → monorepo sibling) | consult, portfolio, trends | Workspace generic discovery | **Retained with no exit** while installed and monorepo layouts coexist | Each wrapper fixture passes in all supported layouts; no migration planned | #1951 |
| Market-overlay sibling resolution ladder inside the W6 helper: `{NAME}_PLUGIN_ROOT` env → newest `~/.claude/plugins/cache/insight-wave/<name>/` → monorepo sibling, a candidate winning only when the overlay file exists under it (`cogni-workspace/scripts/get-market-config.py:55-98`) | every W6 caller that passes `--plugin research` or `--plugin trends` (knowledge, portfolio agents pass research; trends passes trends); an unresolvable overlay yields registry data alone | Workspace merge utility W6 at its current path | **Retained with no exit** while installed-cache and monorepo layouts coexist; the `research` overlay lives in the dormant `cogni-research` tree outside the marketplace roster, so the registry-only fallback is part of the supported contract, not a defect | Overlay-join tests pass in env, cache, sibling and overlay-absent layouts; no migration planned | #1951 |
| Workspace-helper resolution ladders in W6 callers: `WORKSPACE_PLUGIN_ROOT` → monorepo sibling → newest cache (`cogni-knowledge/skills/knowledge-curate/SKILL.md:56-70`, fail-loud on miss); `WORKSPACE_PLUGIN_ROOT` → newest cache (`cogni-knowledge/skills/knowledge-plan/SKILL.md:96,137`, `cogni-knowledge/skills/knowledge-setup/SKILL.md:116`, `cogni-portfolio/agents/market-researcher.md:31`, `cogni-portfolio/agents/customer-researcher.md:30`, `cogni-portfolio/agents/competitor-researcher.md:25`, `cogni-portfolio/scripts/validate-entities.sh:363-367`, fail-soft) | knowledge-curate, knowledge-plan, knowledge-setup, portfolio research agents and entity validator | Retained W6 script at `cogni-workspace/scripts/get-market-config.py` | **Retained with no exit**; publishing never touches W6, so no caller path changes; knowledge's fail-loud abort on a `_default` resolution and portfolio's fail-soft skip both keep their current semantics | `cogni-knowledge/tests/test_skill_contracts.sh:98-100,160-162`, `cogni-knowledge/tests/test_language_config_contract.sh:112` and `cogni-portfolio/tests/test-bash-grant.sh` stay green; a repository scan finds no caller resolving W6 through a publishing path | #1951 |
| Output-register resolution ladder (`COGNI_WORKSPACE_PLUGIN` → `WORKSPACE_PLUGIN_ROOT` → cache/sibling, `cogni-consult/references/user-facing-output.md:17`) | vertical-plugin overlays | Workspace canonical user-facing register | **Retained fail-soft**; missing register degrades guidance, not work | Overlay tests pass for env/cache/sibling/absent cases | #1951 |
| MCP install directories and host config keys | install-mcp and workspace status | Workspace MCP registry and host-specific config | **Retained**; user-controlled installation/config state | Registry and host-state matrix stay tested; no publishing-specific stale requirement remains | #1951 |
| Retired local renderer/story-to-* triggers | no live producer; historical docs/ledger only | New publishing `design-render` capabilities, not an alias-compatible continuation | No delegation: old input/output contracts differ; keep triggers retired | Active dispatch scan stays empty; new routes advertise only their versioned contracts | #1943-#1948, #1951 |

## Bounded source-to-destination path map

Every move or contract migration above is bounded to the path scopes below. A child may touch nothing outside its listed source and destination scopes without amending this record first. Destination skill, command and agent names are identical to today's, so old-name delegation is a same-name dispatch across plugins, never a rename.

| Rows | Source path scope (today) | Destination path scope | Compatibility delegate left in workspace | Consumer-update paths | Preserved inputs → outputs | Witness | Explicit exclusions |
|---|---|---|---|---|---|---|---|
| E1, E4 | `cogni-workspace/skills/text-to-narrative/SKILL.md`, `cogni-workspace/skills/text-to-narrative/scripts/**` (`bridge-citations.py`, `check-design-brief.py`, `validate-narrative.py`), `cogni-workspace/skills/text-to-narrative/evals/**`, `cogni-workspace/commands/text-to-narrative.md`, fixtures under `cogni-workspace/tests/fixtures/design-brief/**` and `cogni-workspace/tests/fixtures/narrative-output/**` | `cogni-publishing/skills/text-to-narrative/**`, `cogni-publishing/commands/text-to-narrative.md`, `cogni-publishing/tests/fixtures/**`; the normalized-brief contract E4 feeds lands under `cogni-publishing/contracts/**` (#1943) | `cogni-workspace/skills/text-to-narrative/SKILL.md` and `cogni-workspace/commands/text-to-narrative.md` reduced to thin delegation until the exit named in the compatibility index | Dispatch sites: `cogni-portfolio/skills/portfolio-communicate/SKILL.md:74-75`, `cogni-trends/skills/trends-resume/SKILL.md:217-227`, `cogni-trends/skills/verify-trend-report/SKILL.md:368`, the executable next-action emitter `cogni-trends/scripts/project-status.sh:867` with `cogni-trends/tests/test-project-status.sh:220`, `cogni-consult/skills/consult-action-fields/SKILL.md:122-136,202`, `cogni-consult/skills/consult-publish/SKILL.md:133`, `cogni-consult/references/publish-routing.md:48-50`, `cogni-marketing/skills/abm/SKILL.md:124`, `cogni-marketing/skills/thought-leadership/SKILL.md:143`. Guidance sites: `cogni-marketing/references/data-model.md:184`, `cogni-marketing/README.md:190`, `cogni-trends/skills/verify-trend-report/references/downstream-options.md`, `cogni-trends/skills/value-modeler/references/workflow-phases/phase-4-rank.md:273`, `cogni-portfolio/skills/portfolio-communicate/references/use-case-registry.md`, plus each of those plugins' README and CLAUDE dependency tables. Arc-contract readers that name the skill without dispatching it belong to E3. The scope is closed by scan: a `text-to-narrative` search over `cogni-{consult,marketing,portfolio,sales,trends,website,knowledge}/**` returns only files on this list or on E3's | source markdown, arc/target options, citations → four ordered arc sections plus sources, QA envelope, frozen design brief per `design-brief-template.md:1-10` | Publishing-only fixture run; brief field comparison lossless; `check-design-brief.py` green on the relocated design-brief fixtures | No other `cogni-workspace/skills/**`; no `cogni-workspace/hooks/**`; no `cogni-workspace/scripts/**` |
| E2 | `cogni-workspace/skills/copywriter/**`, `cogni-workspace/agents/copywriter.md`, `cogni-workspace/commands/copywrite.md`, `cogni-workspace/references/copywriter-changelog.md` | `cogni-publishing/skills/copywriter/**`, `cogni-publishing/agents/copywriter.md`, `cogni-publishing/commands/copywrite.md`, `cogni-publishing/references/copywriter-changelog.md` | `cogni-workspace/skills/copywriter/SKILL.md` and `cogni-workspace/commands/copywrite.md` reduced to thin delegation; `cogni-workspace/agents/copywriter.md` removed only once the delegate no longer dispatches it | Dispatch sites: `cogni-consult/skills/consult-design-thinking/SKILL.md:443-463`, `cogni-consult/skills/consult-publish/SKILL.md:100-103,162,201-214,241`, `cogni-consult/references/publish-routing.md:176-196`, `cogni-marketing/skills/demand-generation/SKILL.md:129`, `cogni-marketing/skills/lead-generation/SKILL.md:131`, `cogni-marketing/skills/thought-leadership/SKILL.md:142`, `cogni-trends/skills/verify-trend-report/SKILL.md:29-35,361-368`, `cogni-trends/skills/trends-resume/SKILL.md:211`, `cogni-trends/scripts/project-status.sh:302-311,750,865` with `cogni-trends/tests/test-project-status.sh:13-21`, `cogni-sales/skills/why-change/SKILL.md:393,418,438`, `cogni-portfolio/skills/portfolio-communicate/SKILL.md:360-393`. Guidance sites: `cogni-marketing/CLAUDE.md:70`, `cogni-marketing/references/data-model.md:183`, `cogni-trends/skills/verify-trend-report/references/downstream-options.md`, `cogni-portfolio/skills/portfolio-communicate/references/**`, `cogni-consult/references/orchestration/test-promote-check.md:18,91,104` (the `copywriter-polish` lineage kind), `cogni-consult/references/user-facing-output.md:206-211` and `cogni-knowledge/references/concise-structure.md:20` (prose naming the copywriter's framework and orthography rules, wording only), plus each plugin's README and CLAUDE dependency tables. The scope is closed by scan: a `copywriter`/`/copywrite` search over the same seven plugin trees returns only files on this list, the one exception being `cogni-website/agents/page-generator.md:109`, where "copywriter-grade" describes portfolio-communicate and names no route | document, scope, language, persona, `arc_id` → in-place polished/translated document and review result | Publishing-only test exercises E1→readability and E2→arc in both directions; the consult design-thinking loop still records `copywriter-polish` as WARN-on-failure | `cogni-workspace/agents/claim-verifier.md` and `cogni-workspace/agents/source-inspector.md` (W8) stay; `cogni-workspace/references/user-facing-output.md` (W2) stays |
| E3 | `cogni-workspace/skills/text-to-narrative/references/**` (arc contracts, `arc-registry.md`, `density-ceilings.md`, `design-brief-template.md`, `language-*.md`, `techniques-overview.md`, `validation.md`, `visual-intent.md`, `execution-brief.md`) and `cogni-workspace/libraries/**` (`arc-taxonomy.md`, `presentation-intent.md`, `infographic-*.md`, `web-section-*.md`) | Skill-private files to `cogni-publishing/skills/text-to-narrative/references/**`; the cross-plugin contracts (arc registry and headings, density ceilings, brief grammar, presentation intent, arc taxonomy) to versioned files under `cogni-publishing/contracts/**` | Compatibility path or delegation for the private references during caller migration, per the compatibility index; no mirrored authority | `cogni-portfolio/skills/portfolio-communicate/SKILL.md:117,130`, `cogni-portfolio/skills/portfolio-communicate/references/templates-pitch.md`, `cogni-portfolio/skills/portfolio-communicate/references/templates-customer-narrative.md`, `cogni-sales/skills/why-change/SKILL.md:25-26,159-260`, `cogni-sales/agents/why-change-researcher.md:52-67`, `cogni-sales/skills/why-change/references/output-specs.md`, `cogni-sales/skills/why-change/references/pitch-data-model.md`, `cogni-trends/agents/trend-report-composer.md`, `cogni-trends/agents/trend-report-investment-theme-writer.md:90-91`, `cogni-trends/skills/trend-synthesis/SKILL.md:116,366-367,541`, `cogni-trends/skills/trend-synthesis/references/**`, `cogni-consult/references/frameworks-registry.md:33`, the `cogni-website/CLAUDE.md:40` arc-taxonomy reference, the consult `presentation-intent.md` parity copy named in the compatibility index | `arc_id`, evidence, target → byte-identical headings, composition/validation rules, visual intent, brief grammar | The arc-reference sync suite (today `cogni-workspace/tests/test-arc-reference-sync.sh`) runs from publishing; parity fixtures unchanged | `cogni-workspace/references/**` other than `copywriter-changelog.md`; `cogni-workspace/libraries/**` holds no non-editorial file, so the directory empties rather than being split |
| T1 | `cogni-workspace/skills/manage-themes/**`, `cogni-workspace/themes/**` (`_template`, `boardroom`, `clean-slate`, `cogni-work`, `editorial`, `signal`), the theme scripts `discover-themes.py`, `inspect-themes.py`, `validate-theme-manifest.py`, `sanitize-theme.py`, `load-theme-component.py`, `generate-tokens-css.py`, `check-theme-drift.py`, `check-contrast.py`, `import-claude-design-bundle.py`, `verify-claude-design-importer.sh` and `verify-theme-backcompat.sh` under `cogni-workspace/scripts/`, the theme references `theme-manifest.md`, `theme-manifest.schema.json`, `theme-component-loader.md`, `claude-design-bundle-mapping.md` and `design-variables-pattern.md` under `cogni-workspace/references/`, and `cogni-workspace/docs/theme-system-v2-migration.md` | `cogni-publishing/skills/manage-themes/**`, `cogni-publishing/themes/**`, `cogni-publishing/scripts/**`, `cogni-publishing/references/**`, `cogni-publishing/docs/**` | `cogni-workspace/skills/manage-themes/SKILL.md` reduced to thin delegation with identical operations, arguments and result fields | `cogni-consult/skills/consult-dashboard/**`, `cogni-portfolio/skills/portfolio-dashboard/**`, `cogni-trends/skills/trends-dashboard/**`, `cogni-workspace/skills/workspace-dashboard/**`, `cogni-website/skills/website-setup/SKILL.md`, `cogni-website/skills/website-build/**`, the consult, portfolio and trends dashboard-refresher agents, `cogni-website/agents/hero-renderer.md`, `cogni-website/agents/site-assembler.md`. No voice consumer path exists; the harness's `VOICE_PLUGINS=(cogni-sales)` entry at `verify-theme-backcompat.sh:455` moves with the harness unchanged, and reconciling it against a live-reader scan is #1951 wording residue, not a #1950 caller migration | bundle / local theme / explicit path → canonical saved theme, `theme_path`, `theme_name`, `theme_slug`, tiers and tokens | The backcompat inventory at `verify-theme-backcompat.sh:122-132,353-388` passes from a publishing-only install; alias and re-import suites green | User theme directories are read in place and never moved. Every other file under `cogni-workspace/scripts/` stays: `get-market-config.py`, `discover-plugin-projects.sh`, `discover-plugins.sh`, `generate-settings.sh`, `install-mcp.sh`, `patch-desktop-config.py`, `setup-obsidian.sh`, `update-obsidian.sh`, `install-workspace-deps.sh`, `check-dependencies.sh`, `check-market-orphans.py`, `check-skill-names.sh`, `check-workspace-python-deps.sh`, `mutation-check.sh` and `baselines/` |
| T2 | No source move: saved themes and each consumer's `design-variables.json` stay at their current project paths | One versioned artifact contract file, `theme-artifact@1` at `cogni-publishing/references/theme-artifact-contract.md` (the plugin's contracts live under `references/`, the layout its founding change landed), describing the saved theme, its tokens and the `design-variables.json` shape | None: consumers read saved artifacts directly | Each consumer path listed for T1 switches its documented contract reference only; generated outputs keep their paths | selected theme or `design-variables.json` → consumer-owned CSS/HTML/voice | `theme_path`/`theme_name`/`theme_slug` precedence fixtures pass unchanged without invoking authoring | No consumer generator script moves; no user artifact is rewritten |
| M1 | `.claude-plugin/marketplace.json:54-65`, `cogni-workspace/.claude-plugin/plugin.json:1-24`, `cogni-workspace/README.md`, the root `README.md`, `docs/**` dependency tables and `assets/architecture.*` | A new `cogni-publishing/.claude-plugin/plugin.json` plus its marketplace entry (#1943); reduced workspace wording (#1951) | None | `docs/command-reference.md`, `docs/plugin-selection.md`, `docs/ecosystem-overview.md`, `assets/architecture.excalidraw` and `assets/architecture.svg`, each dependent plugin's README dependency table | metadata and guidance → discoverability | Manifest-sync, README inventory-sync and command-reference guards green; the version-bump gate reports no feature-branch edit | `version` values and maturity keywords |

### T1 and T2 amendment (recorded with #1944)

The T1 move forced touches outside the scopes above. Each is recorded here, as this record requires, rather than taken silently:

- **Source scope, additions.** `cogni-workspace/scripts/baselines/README.md` and `_template-tier0-output.json` — that directory held only the tier-0 snapshot `verify-theme-backcompat.sh` diffs against, so it moves with the harness to `cogni-publishing/scripts/baselines/`; the T1 exclusion "`baselines/` stays" is superseded. The theme suites `cogni-workspace/tests/test-theme-backcompat.sh`, `test-bundled-presets.sh` and `test-check-contrast.sh` move to `cogni-publishing/tests/`, which joins the destination scope with new `test-theme-lifecycle.sh`, `test-semantic-tokens.sh` and their fixtures.
- **Compatibility delegates, beyond the skill.** Four script paths keep live callers outside the move and stay as delegation-only entry points: `discover-themes.py`, `inspect-themes.py` and `check-theme-drift.py` (run by `workspace-status`) and `sanitize-theme.py` (imported by path by the `workspace-dashboard` renderer). All four resolve cogni-publishing through one resolver, `cogni-workspace/scripts/_publishing_delegate.py` — `$COGNI_PUBLISHING_PLUGIN`, then the monorepo sibling, then the newest plugin cache — and fail with an install-guidance envelope when it is absent. `cogni-workspace/references/design-variables-pattern.md` stays as a pointer for its prose readers. `cogni-workspace/tests/test-theme-compat-delegates.sh` holds every delegate to delegation-only shape and proves each arrives.
- **Workspace consumers repaired so nothing breaks before callers migrate.** `manage-workspace` seeds `_template/` from the cogni-publishing bundle through the resolver, fail-soft; `workspace-status` names the bundled themes and manifest reference by owner rather than by a `${CLAUDE_PLUGIN_ROOT}` path that no longer resolves; the `workspace-dashboard` generator also scans `cogni-publishing/themes` in monorepo dev mode; `test-relocated-skill-hygiene.sh` drops its two moved loader rows, whose forbidden-token check `test-theme-lifecycle.sh` carries forward.
- **Same-name delegation made mechanically legal.** `cogni-workspace/scripts/check-skill-names.sh` accepts a `<!-- compatibility-delegate: <plugin> -->` marker, and only while `<plugin>` ships the skill; its suite gains the three cases.
- **Wording that the move made false.** The theme sections of `cogni-workspace/CLAUDE.md` and `README.md`, cogni-publishing's `CLAUDE.md` and `README.md`, the root `CLAUDE.md` theme-inheritance bullet, the root `README.md` skill counts and `docs/command-reference.md`. Positioning wording elsewhere is left to #1951.

Not touched, as this record assigns them later: the consumer caller paths listed in the T1 row (#1950), and the workspace and marketplace descriptions (#1951).

## Roadmap child execution map

| Child | Executable scope inherited from this record | Preserved contract / explicit exclusion |
|---|---|---|
| #1943 | Create `cogni-publishing`, marketplace entry, versioned normalized brief/composition/target-plan/design-system/provenance/result contracts and standalone validators | Accept existing narrative briefs losslessly and structured direct briefs without arc rewriting; preferences optional; render runtimes not provisioned by validation |
| #1944 | Move T1 completely within the T1 source and destination scopes of the path map and establish T2's public artifact contract, semantic aliases and compatibility projection | One lifecycle owner; preserve tier-0/tiered/explicit/user paths and `theme_path`/`theme_name`/`theme_slug`; no overwrite of user assets |
| #1945 | Compose frozen normalized units with reusable semantic patterns and reference-completeness/provenance validation | No target geometry as interchange, no copy rewrite, no invented quantitative data |
| #1946 | Render validated composition to portable branded HTML through a separately pinned runtime | Preserve strings, notes, sources, reading order and declared accessibility semantics; no model/API or workspace prerequisite |
| #1947 | Render the same composition directly to editable PPTX with recorded object editability | No HTML intermediary; native text/shapes/charts where promised; explicit SVG fallback only |
| #1948 | Prove both renderers with two brands, deterministic fidelity checks, full-resolution visual review and bounded content-preserving repair | No averaged-away critical defects; no proprietary service prerequisite |
| #1949 | Move E1-E4 as one publishing-owned editorial subsystem with shared local assets and old-name delegation, within the E1-E4 source and destination scopes of the path map | Preserve polish/translation/compression/arc/freeze semantics; consult direct briefs remain outside mandatory narrative; no post-freeze rewrite |
| #1950 | Migrate every inventoried publishing caller, private reference and active user-facing route to public publishing contracts, along the consumer-update paths of the path map (marketing callers, the consult-publish optional polish and the trends next-action emitter included) | Preserve optional fallback, consult `publish[]` provenance and supported-format boundary; do not change W6-W8 or other non-publishing workspace calls |
| #1951 | Remove only residue whose compatibility exit is met; reconcile W1-W12, R1 and M1 across setup, health, help, diagrams and migration/rollback guidance | Retain claims, markets, discovery, settings, MCP, diagnostics and Obsidian unless a separate replacement has actually landed; no feature-branch version bump |

**Current HTML boundary.** Host-authored HTML follows the publishing DOM contract and passes independent verification. The separately pinned browser runtime measures output only; it is provisioned by the operator, never by validation or rendering.

**Current PPTX boundary.** The host presentation skill creates native editable output directly from the frozen brief and composition. The independent verifier reads the package itself, then preservation and visual review gate handover. The repository ships no built-in artifact writer.

## E1–E4 editorial-transfer amendment

The E3 destination discrepancy is settled in favor of the location already established by T2: all public editorial contracts live at versioned addresses under `cogni-publishing/references/**`, indexed by `editorial-contract-index.md`. No `contracts/**` mirror is created. The canonical narrative and copywriter implementations, scripts, evals, agent, commands, six shared libraries, six suites and four fixture trees move together to publishing. Workspace retains only same-name skill/command delegates and the 31 bounded compatibility pointers required by unmigrated callers; consumer migration remains the next child.

Three narrow guard repairs sit outside the E1–E4 source/destination rows and are authorized here because coverage must follow the moved authority:

- `cogni-consult/tests/test-presentation-intent-sync.sh` compares its self-contained shared block with `cogni-publishing/references/presentation-intent.md`, including its nested mutant, while preserving `pi-sync-03`.
- `cogni-workspace/tests/test-relocated-skill-hygiene.sh` points the existing copywriter adoption rows at the publishing implementation and agent.
- `cogni-workspace/tests/test-de-ascii-orthography.sh` scans the publishing plugin by default so the transferred German editorial corpus remains covered.

The normal E4 continuation is now normalize → design-compose → design-render, with the design-brief fingerprint checked before and after. Claude Design remains an optional handoff. Neither route may invoke a copy-fit rewrite after Phase 7 freezes the brief.

## Reconciliation and supported end state

All issue-named responsibilities are classified: narrative/readability/copywriter/arc and themes move; claims, markets, project discovery, preferences, plugin/tool/MCP setup, health and compatibility paths without safe exits remain supported; consult-native briefs remain direct; the old producerless renderer remains removed. Executable calls, dispatches, private references, configuration, artifacts, hooks, tests, manifests and user guidance are mapped to an owner and witness.

After #1951, `cogni-workspace` survives as a smaller, supported horizontal infrastructure plugin. It owns W1–W12 and no publishing implementation. `cogni-publishing` owns E1–E4 and T1–T2's canonical contracts/lifecycle plus the new composition/render/verification chain. Business plugins own their domain content and consume public publishing contracts or saved artifacts. No required decision is left for a later approval gate.

This phase removes no behavior, changes no manifest, and redesigns no unrelated subsystem. Later children must treat this record as a closed ownership boundary: a proposed deviation either supplies a concrete migrated replacement and passing witness or remains with the decided owner.

## Consumer-migration reconciliation

The #1950 consumer pass switches active business-plugin dispatches, theme reads,
editorial-contract reads, next actions, and current user guidance to public
`cogni-publishing` capabilities. It does not rewrite the historical source-side
paths recorded above: those remain the evidence of what moved. Compatibility
delegates in cogni-workspace also remain until their separate exit gate.

The pass reconciles every touched surface, including paths that were not named
verbatim in the original consumer rows:

| Surface class | Current state | Witness / exit |
|---|---|---|
| Consult publishing routes, route lineage, and dashboard theme lookup | Public publishing dispatches and references; one `local-render:<target>` lineage step; bundled plus user `<workspace-root>/themes/` lookup | `cogni-consult/tests/test-publishing-routes.sh`; no compatibility exit remains |
| Portfolio dashboard theme lookup | Public bundled themes plus user `<workspace-root>/themes/` | Root consumer guard; no compatibility exit remains |
| Sales arc readers and Why Change orchestration | Resolve the cogni-publishing plugin root and public arc contracts | Root consumer guard; no compatibility exit remains |
| Trends verification prerequisite | `cogni-publishing:copywriter` is the optional prerequisite | Root consumer guard; no compatibility exit remains |
| Website setup and workspace dashboard | Setup persists `theme_path`, `theme_name`, and `theme_slug`; dashboard reads publishing's public design-variable contract | Root consumer guard; no compatibility exit remains |
| Active ecosystem, plugin-guide, and workflow prose | Publishing owns editorial/theme/local-render routes; document and infographic remain Claude Design handoffs | Root consumer guard scans the named active files; historical planning records remain intentionally unchanged |

The only retained cogni-workspace publishing routes are the same-name
compatibility delegates and pointer files established by the transfer
amendments. Their exit remains the separately gated residue cleanup: remove a
delegate only after the active-dispatch scan is empty and its publishing-owned
replacement passes the corresponding parity suite. Supported workspace-owned
claims, markets, discovery, settings, MCP, health, and Obsidian paths are not
compatibility residue and therefore have no removal exit.

The compatibility-index status for this pass is: E1/E2 active dispatches
migrated; E3 active private-reference readers migrated; T1 active theme
consumers migrated; T2 saved artifact locations retained by design. The root
consumer guard covers qualified dispatches, private editorial paths, private
theme/design-variable paths, and bare legacy commands, with a planted falsifier
for each class.

Consult keeps its Pyramid/SCQA/MECE brief native. Slides and web-posters may,
after assumption resolution and explicit election, enter publishing's direct
brief normalize → compose → render chain (`pptx` and `html` respectively).
Report and infographic remain Claude Design handoffs. Every route retains
`brief_path`; a successful local render adds `artifact_path` without replacing
source provenance.

## Closure: reconciled inventory and declared migration window

This section closes the transition on the workspace side. It adds no row to the
tables above and amends none of them; it records the owner and the verified live
consumers of each retained responsibility, states the measured witness behind
each compatibility-route exit, and declares the migration window three of those
exits name.

### Retained responsibilities and their verified consumers

Every row below is **retained** and owned by `cogni-workspace`. None has a
landed replacement, and age or host overlap is not evidence for removal.

| Row | Responsibility | Verified live consumers |
|---|---|---|
| W1 | Workspace preferences and generated settings | `manage-workspace`, `generate-settings.sh`, the `SessionStart` hook; every plugin that sources `.workspace-env.sh` |
| W2 | Language delivery and the user-facing output register | `hooks/on-session-start-language.sh`; the consult overlay at `cogni-consult/references/user-facing-output.md` |
| W3 | Plugin and optional-tool discovery | `discover-plugins.sh`; `cogni-consult` dispatch availability checks |
| W4 | Dependency provisioning | `manage-workspace`, `workspace-status` |
| W5 | MCP installation and host config | `install-mcp`, `patch-desktop-config.py`, `workspace-status` |
| W6 | Supported-markets registry and the overlay merge utility | `get-market-config.py` callers in cogni-knowledge, cogni-portfolio agents and cogni-trends |
| W7 | Shared project discovery | `discover-plugin-projects.sh` wrappers in cogni-portfolio, cogni-consult and cogni-trends |
| W8 | Claim verification | `claims`, `claim-verifier`, `source-inspector`; submissions from cogni-trends, cogni-portfolio, cogni-consult and cogni-knowledge's opt-in resweep |
| W9 | Workspace health and diagnostics | `workspace-status`, `workspace-dashboard`, `/troubleshoot` |
| W10 | Obsidian integration | `setup-obsidian.sh`, `update-obsidian.sh` |
| W11 | Issue reporting | `cogni-issues` |
| W12 | Output style register | `output-styles/workspace-advisor.md`, discovered at the plugin root |
| R1 | The producerless local render chain | **Remains removed.** No producer exists and none is proposed; cogni-publishing's chain is a new contract, not a continuation |
| M1 | Manifest aliases | **None exist.** The class stays explicit here so a future alias must record its callers and removal condition before it ships |

No ownership row is left unresolved, and the documented outcome matches the tree:
`cogni-workspace` survives as a smaller horizontal infrastructure plugin rather
than retiring.

### Measured exit witnesses

The consumer-scan half of each compatibility-route exit was re-measured after
the consumer migration landed, by scanning the seven consumer plugin trees
(`cogni-consult`, `cogni-marketing`, `cogni-portfolio`, `cogni-sales`,
`cogni-trends`, `cogni-website`, `cogni-knowledge`):

| Route | Scanned tokens | Files with hits | Verdict |
|---|---|---|---|
| Editorial narrative | `cogni-workspace:text-to-narrative`, `/text-to-narrative` | 0 | consumer-scan half **satisfied** |
| Editorial copy | `cogni-workspace:copywriter`, `/copywrite` | 1 | consumer-scan half **satisfied** — the single hit is a mutation *string* inside a recipe comment in `cogni-trends/tests/test-project-status.sh`, not a dispatch |
| Theme lifecycle | `cogni-workspace:manage-themes`, `/manage-themes` | 0 | consumer-scan half **satisfied** |

### Declared migration window

Three exit conditions above name a migration window as their remaining half.
**The window opens when this record lands and closes 2026-12-15.**

Through the window, the same-name `cogni-workspace` routes for
`text-to-narrative`, `copywriter` and `manage-themes` keep working unchanged and
are **retained, not removed**. A window cannot have elapsed at the moment it is
declared, so no route reaches its removal state in the change that declares it;
removing them is the window's own dated successor, which re-measures the three
scans above as fresh witnesses before deleting anything.

`docs/publishing-migration.md` is the user-facing guidance those exit conditions
require, and is the surface a user reads. This record remains the decision
surface.

### Rows that keep no exit

The rows marked **retained with no exit** in the compatibility-route index are
unchanged by this closure and acquire no exit here: the `{working_dir}/cogni-claims/`
data path, the project-wrapper resolution ladders, the market-overlay sibling
ladder inside the W6 helper, the workspace-helper ladders in W6 callers, the
output-register resolution ladder, and the MCP install directories and host
config keys. Each supports a layout or a body of user data that cannot be
migrated safely, and no removal is promised for any of them.
